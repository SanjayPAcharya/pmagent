# AgentPM — Implementation Progress

> **Live status of the monorepo build.** The plan (what to build) lives in [`agentpm-plan/`](agentpm-plan/README.md); the code lives in [`sourcecode/`](sourcecode/README.md) (the pnpm workspace root — all paths below are relative to it). This file tracks **what's actually been done**. Load this first each session to see where things stand.

## How to use this file (the convention)

- **Update it after every implemented step** — same commit as the code. A "step" = one checkbox below.
- Status markers: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked.
- When you finish a step: tick its box, and add a one-line entry to the **Log** (newest first) with the date + commit/PR.
- Keep **Now / Next / Blocked** current — it's the 5-second answer to "where are we?".
- Don't duplicate plan detail here; link to the phase file for the how.

---

## Now / Next / Blocked

- **Current phase:** the **3.x feature-rich-&-simple track** on the `dev` branch (merge to `main` manually to trigger the deploy pipeline). ✅ Complete: Phases 1 · 2 · 2.1 · 2.5 · 2.6 · 2.8 · 2.8.5 · **3 (prod LIVE on EC2 + CI/CD)** · org/project redesign (tree rail, enriched cards, activity) · **3.1 PM depth** · **3.2 collaboration C1–C3** (C4 attachments deferred) · **3.7 review-driven planning surfaces (Overview · Gantt · quick-create · workstreams · release readiness) — DONE, all R0–R14 shipped & browser-verified, unpushed on `dev`**. Phase 5.5 agent-first UI stays parked → lands with Phase 5.
- **Now (2026-07-07):** **Phase 3.7.2 UI/UX polish — ✅ DONE, all P1–P6 landed** (`agentpm-plan/phases/phase-3.7.2-ui-ux-polish.md`; 6 step commits + the P0 doc, local on `dev`). From a full UX audit (verdict: genuinely user-friendly; two suspected gaps — slash-command discoverability, column-width persistence — already solved). Web-only, api stayed 77/77, **web 31→32**: **P1** inline field validation (`aria-invalid` red border via the arbitrary-variant `aria-[invalid=true]:` since `aria-invalid` isn't a default Tailwind variant, + `FieldError`; Account name, drawer date range) · **P3** blocked badge → shared `BlockedBadge` on a brightened dark `--destructive` (5 hardcoded-red sites) · **P4** `Loader2` busy feedback on 4 async Saves (fixes double-submit) · **P2** shared `EmptyState` across 6 call sites (+1 test) · **P5** bulk-archive confirm count + 4s reset + spinner · **P6** global `:focus-visible` ring for naked interactive elements. Every step browser-verified in dark mode; typecheck + vite build green throughout. Owner constraint honoured: no Jira/Monday cloning, no modal/form-lib/illustrations. *(Two follow-ups surfaced: (1) the drawer collapsed-Relationships blocked badge is dormant — `api.getTicket` returns no blocker count — flagged as a background task; (2) a throwaway RELA-6 remains archived in Oracle → Relationship Manager, owner opted to leave it.)*
- **Prior (2026-07-06):** **Phase 3.7.1 gap closure ✅ DONE** — all 6 audit follow-ups shipped and browser-verified (7 commits incl. F0 docs; api 77/77 · web 31/31 · typechecks + vite build green): **F1** Start date in the ticket drawer · **F2** milestone rename/re-date/delete UI (inline confirm; Gantt lane follows) · **F3** expanded-sprint live sync (`['sprint']` prefix; Redis-verified) · **F4** subtask "done/total" chips on board cards (R9.4) · **F5** List Due column (R13) · **F6** Gantt rail narrows below 640px (R7.7). All test data restored (subtasks deleted + counter reset; milestone removed; column pref reverted). Plan: `agentpm-plan/phases/phase-3.7.1-gap-closure.md`. This came after the 3.7 audit confirmed the phase core genuinely done (all R0–R14 verified, multi-select filters browser-checked).
- **Prior (2026-07-05):** **Phase 3.7 — review-driven planning surfaces — ✅ DONE** (all R0–R14) and **pushed to `origin/dev`** after Sanjay's review (also includes a follow-up: multi-select board + list filters). Shipped this session (14 phase commits + 2 filter commits + docs): Milestone model + `startDate`/`workstream` schema & rules (R1–R2) · `useProjectSync` live sync (R3) · **Overview dashboard as the project landing**, Board moved to `/board` with legacy `/ticket/:n` kept (R4–R5) · **Gantt/Timeline** — tested UTC-day math, read-only chart, drag move/resize/tray-schedule/milestone + undo (R6–R8) · quick-add tokens + inline subtasks (R9) · inert Beta AI affordances (R10) · **sprint-vs-ad-hoc workstream** across board/list tabs, drawer, bulk, CSV, reports (R11) · sprint view inline actions + goal edit (R12) · list column chooser + resizable widths (R13) · **release readiness** on Reports (R14). **api 75/75 · web 31/31**, typecheck + vite build green; every UI step browser-verified with all test data restored. Plan: `agentpm-plan/phases/phase-3.7-review-feedback.md`. AI asks remain deferred to after Phase 5 (Beta-tag affordances only).
- **Now (2026-07-07, cont.):** **Phase 3.7.3 Archive & Restore — ✅ COMPLETE** (`agentpm-plan/phases/phase-3.7.3-archive-restore.md`; owner request: "a place where archived tickets or projects can be seen, or retrieved"). Delivered both, each via a **dedicated Archived page**: **tickets** (per-project `/…/archived` — restore + permanent delete; new `archivedOnly` filter + `DELETE /:id/permanent`) and **projects** (`Project.archivedAt` migration; archive/restore endpoints; excluded from every listing — org count, sidebar stats, search, my-work; per-org `/orgs/:slug/archived` page; Danger Zone now archives, recoverable). **api 81/81 · web 32/32**, all browser-verified (incl. finally purging the RELA-6 throwaway and restoring the mis-archived RELA-5). All commits local on `dev` (unpushed).
- **Now (2026-07-08):** **Phase 3.7.4 compliance & security hardening — ✅ COMPLETE** (all A/B/C/D/E; `agentpm-plan/phases/phase-3.7.4-compliance-hardening.md`), from the standards audit (`release-doc/ARCHITECTURE-AND-COMPLIANCE-REPORT.md`). Landed: **C** append-only `AuditLog` (no FK) at 8 call sites + ADMIN `GET /api/orgs/:slug/audit`; **B** `GET /api/me/export` (GDPR Art. 20) + `DELETE /api/me` (Art. 17 erasure-by-anonymization, sole-owner-guarded) + Account Settings UI, browser-verified; **A** `@fastify/helmet` baseline headers + Caddy HSTS/CSP-Report-Only; **D** CI `pnpm audit` gate + Dependabot (**caught & fixed 5 real CVEs** on the JWT path — `@fastify/jwt` 9→10, `get-jwks` 9→11) + Redis-backed & tiered rate limits (ioredis); **E** axe-core e2e a11y gate (**found & fixed 3 real defects incl. 2 critical**; token-level contrast fixes) + daily retention purge job. **api 81 → 89 · web 32/32**, typecheck + build green throughout; every step committed locally on `dev` (unpushed). **Two deploy-time follow-ups noted in the phase file:** promote the Caddy CSP from Report-Only after checking prod console; e2e a11y gate not yet wired into CI (needs playwright + seeded KC user via `infra/keycloak/seed-e2e-user.sh`).
- **Now (2026-07-11):** **Phase 3.8 — AI ticket drafting via Amazon Bedrock — ✅ COMPLETE, live on prod** (plan: `agentpm-plan/phases/phase-3.8-ai-tickets.md`, renamed from `…-local-ai-tickets.md`). **Owner decision (2026-07-09): drop the self-hosted Ollama tier entirely; v1 = Bedrock Amazon Nova Micro from ap-south-1** (APAC inference profile, ~₹10–40/mo at expected volume) — if quality disappoints, switch to **Claude Haiku 4.5** (`global.*` profile, ≈₹320/mo) is a pure env-var flip through the same `AIProvider` seam. No EC2 resize (t3.medium stays; old C2/C3 cancelled, never executed); owner accepts ticket text leaving the product for MVP. **Done & permanent:** Part A (seam + 3 endpoints + health, hermetic, api 89→**104**) · Part B (all three buttons live, web 32→**37**) · CI rate-limit fix (owner merged to main). **Part D code ✅ DONE (D1–D4, 2026-07-09):** `BedrockProvider` (Converse, forced-tool JSON) behind the seam · hermetic AWS-SDK-mocked tests (api 104→**106**) · Ollama surface fully removed (provider, compose service+volume, env, FEATURES "self-hosted" wording → honest cloud-AI copy + privacy note) · copy/timeout tuned. **D5 live-verify ✅ (automatable side, 2026-07-10):** AWS dev checklist done (IAM user `pmagent-dev-bedrock`, key in `sourcecode/.env`; profile `apac.amazon.nova-micro-v1:0` **ACTIVE**). Restarted api → drove **all three flows end-to-end through the full stack** against real Nova Micro: health green (`{enabled,reachable,modelReady,provider:bedrock}`); draft-ticket **1.25s** (valid title/desc/6 AC/priority), project-summary **1.05s** (valid digest), expand-ticket **1.67s** (valid desc/goal/constraints). Forced-tool Converse confirmed working (earlier hang was a transient retry storm, not a rejection). **Prompt-tune done (owner chose one pass, stays on Nova Micro):** expand-ticket had returned `acceptanceCriteria: []` on thin tickets → fixed by **`.min(1)` on the AC zod** (draft + expand) so an empty list triggers the seam's corrective re-prompt; 3/3 live re-runs then returned 5 solid AC bullets (~2–2.6s). **Part D ✅ fully done (api 106 → 107, web 37, full suite green).** **Part E ✅ DONE — AI live on prod (2026-07-11).** Shipped via the CI/CD pipeline with a deliberate deviation from the plan's instance-role approach: credentials flow as **GitHub Actions secrets → written into the box's `.env.prod` at deploy time** by `deploy.yml`, which now also **`scp`-syncs the compose files** to the box. (Two root causes of the initial "AI not available": the pipeline shipped images only, so the AI/AWS compose env-passthrough never reached the box — `MODEL` came back empty; and no creds were on the box. The secrets→`.env.prod` + compose-sync deploy step fixes both, hands-free, on every deploy.) Prod `ai/health` green; all three AI features working on `pmagent.sanjaykumarp.info`. **Deferred hardening (non-blocking):** swap the dev IAM-user key for the EC2 **instance role** (original E1 — no keys on the box), add the **$5 budget alert**, and **rotate** the current key (briefly exposed during setup — never reached GitHub, push-protection caught it). Next: **Phase 3.8.1 — AI polish (output quality + UX), 🚧 IN PROGRESS 2026-07-11** (`agentpm-plan/phases/phase-3.8.1-ai-polish.md`; BYOK renumbered → **3.8.2**) — **B1 done** (truthful per-code error copy, web 37→43) · **A1 done** (prompts → `ai.prompts.ts` + offline eval harness `ai:eval` + baseline) · **A2 done** (few-shot measured & dropped; temp/maxTokens + directives kept, PROMPT_VERSION 2) · **A3 done** (context enrichment — 100% first-try + invention down) · **A6 done** (one structured telemetry line per generation; api 107→**114**). **Part A quality work complete** except A4 (model A/B, pairs with ⛔A5 owner decision). **B2 done 2026-07-12** (skeleton + staged hint + cancel + UI-side staged "streaming" reveal — owner asked for a streaming feel; real token streaming stays ruled out per the B2 decision; web 43→48, browser-verified live). **B3 done 2026-07-12** (editable draft preview + per-AC checkboxes + regenerate; auto-fill streamed too; web 48→54). **B4+B5 done 2026-07-12** (auto-fill inverted to a per-field current-vs-proposed review — no upfront confirm; modern "thinking/cooking/finishing" shimmer loader replaces all skeletons; i18n+a11y sweep; web 54→60, browser-verified live). **Part B (generation UX) complete.** **A4 done 2026-07-12** (4-Nova model A/B + ₹/mo table in `release-doc/ai-eval-report.md`; Claude gated by the Anthropic use-case form). **3.8.1 owner decisions in (2026-07-12): ⛔A5** → stay on Nova Micro for now (owner will submit the Anthropic use-case form later, then revisit Haiku 4.5); **⛔C0** → build **T1 subtasks · S1 sprint goal · X1 NL quick-add** → new **Phase 3.8.3 — AI auto-populate** (`agentpm-plan/phases/phase-3.8.3-ai-autopopulate.md`; BYOK stays 3.8.2). **3.8.1 fully closed. Phase 3.8.3 🚧 in progress:** **S1 (sprint goal) done 2026-07-13** (api 114→116, browser-verified live). Next **T1 (subtasks) → X1 (NL quick-add)**, then **A4** → ⛔A5 → ⛔C0 triage. Then **3.6 Part A** / Phase 4/5.
- **Now (2026-07-16):** **Phase 3.8.4 — beta-feedback polish & bug fixes — 🚧 OPENED** (`agentpm-plan/phases/phase-3.8.4-beta-polish.md`), from the first external beta test: 19 findings triaged into `~/Downloads/PMAgent-Beta-Requirements.docx` (v0.2 — owner added **O1** drawer-in-place on Timeline and **O2** dependency visibility on Timeline). Steps: **B1 ✅ done 2026-07-16** (Gantt ticket route + in-place drawer, browser-verified) · **B2 ✅ done 2026-07-16** (milestone chip strip + off-screen arrows, browser-verified) · **B3 ✅ done 2026-07-16** (arrows for scheduled pairs + off-chart glyph/tray dot/legend, browser-verified) · **B4 ✅ done 2026-07-16** (pointerWithin collision + earlier auto-scroll → off-screen Blocked drops land, browser-verified) · **B5 ✅ done 2026-07-16** (createFromTemplate inherits ad-hoc via shared workstreamForTab helper, browser-verified) · **B6 ✅ done 2026-07-16** (honest "N of M tickets done" + "No tickets due" empty state + tooltip, browser-verified; Milestones v2 = 3.8.5 candidate) · **U1** sprints-page layout + no-drag add · **U2** sprint chips on Board cards · **U3 ✅ done 2026-07-16** (List label "+N" overflow chip, browser-verified) · **V** close-out. Root causes pre-diagnosed with line anchors in the phase doc; no schema changes. Baselines: api **118** · web **63**. **Sequencing decided (2026-07-16, owner): run 3.8.4 in full → then 3.8.3 T1 (AI subtasks) → X1 (AI NL quick-add), as one continuous effort but kept as two phases (bugs before net-new AI; different verification modes; beta-tester-facing batch handed back complete first).** ⛔DEC-1 (sprint-board model) and ⛔DEC-2 (action-oriented Overview) await owner decisions.
- **Blocked:** none. Dev-stack gotchas: **after a Prisma schema change**, `docker compose exec api pnpm --filter @agentpm/api exec prisma generate && docker compose restart api` — host-side generate doesn't reach the container's `node_modules` (this is what broke comments on 2026-07-02). **New api source files** also need `docker compose restart api` (tsx watch misses them; edits to existing files are fine after a restart too — macOS bind-mount inotify). After changing deps: rebuild the container. `corepack` flake — pin `corepack pnpm@9.12.0`. Realtime tests need Redis (host `:6379`).

---

## Phase 1 — Skeleton, Auth (Keycloak), Platform → [plan](agentpm-plan/phases/phase-1-skeleton-auth-platform.md)
**Status:** ✅ **COMPLETE (Stages A–E)** — boots, auth, platform CRUD, frontend, tests green. (Optional: social IdP external apps.)

Scaffold & data
- [x] Monorepo: pnpm workspaces + Turborepo (`package.json`, `pnpm-workspace.yaml`, `turbo.json`)
- [x] Prisma schema + first migration (User, Organization, OrgMember, Project) — applied to Postgres

Local container stack (so `docker compose up` works)
- [x] `apps/api/Dockerfile` + `apps/web/Dockerfile`
- [x] `docker-compose.yml` (base) + `docker-compose.override.yml` (dev) + `.env.example`
- [x] Postgres init script (`agentpm` + `keycloak` DBs)
- [x] Local Keycloak container + committed `realm-agentpm.json` (clients, audience mapper, self-registration)

Backend
- [x] Fastify bootstrap + middleware — CORS, rate-limit, websocket, `/health`, **JWKS token verification (iss + aud)**
- [x] Auth middleware (`requireAuth` + JIT user provisioning, `requireOrgRole`) + RBAC
- [x] `GET`/`PATCH /api/me`
- [x] Organizations CRUD + member management
- [x] Projects CRUD (no GitHub repo link yet)

Frontend
- [x] Vite + React 18 + TS + Tailwind (shadcn/ui deferred — plain Tailwind for now)
- [x] React Router + protected layout / auth gate
- [x] keycloak-js auth (login, signup, PKCE, token auto-refresh; social shown on the KC page once IdPs added)
- [x] Dashboard + org/project navigation
- [x] Typed API client (attaches token + refresh-before / retry-on-401)

Identity (external prereqs)
- [x] Keycloak realm running locally (email/password first)
- [ ] Google OAuth client · Azure App Registration · GitHub OAuth App → wired as Keycloak IdPs (optional)

Tests (Stage E)
- [x] Hermetic auth harness (RSA keypair + static JWKS stand-in; no Keycloak in CI)
- [x] API tests green — auth middleware (6), organizations (4), projects (2), health (1) = 13

**Exit:** sign up (email/Google/Microsoft/GitHub) → create org → create project; `docker compose up` runs the full stack; auth + org/project tests pass.

---

## Phase 2 — PM Core (tickets, board, sprints, realtime) → [plan](agentpm-plan/phases/phase-2-pm-core.md)
**Status:** ✅ **COMPLETE (2A–2E)** — tickets/board/sprints/realtime/notifications/invites, verified in-browser.
- [x] **2A** Migration: ticket/sprint/label(+org rel)/comment + `TicketWatcher`/`TicketActivity`/`OrgInvite`/in-app `Notification`; ticket `dueDate`/`archivedAt`; `Project.key`+`ticketCounter` (+backfill); onDelete clauses; project-create derives key; idempotent seed; test truncation extended
- [x] **2B** Tickets backend: transactional create + atomic numbering, `updateTicket` service + activity, comments, assignee/watchers, cross-scope validation, search/filter/sort (whitelist) + cursor pagination, Swagger (zod-provider), `/ready` + graceful shutdown
- [x] **2C** Sprints + completion counts; event bus init/dispose; WS server (project+user rooms, presence, hardened handshake) + shared `WSMessage`; caller-scoped in-app notifications; org invite tokens (role-capped, single-use)
- [x] **2D** Frontend foundation: **shadcn/ui**, routing restructure (public `/invite/:token` vs gated), members endpoint + `api.listMembers`, typed WS client (refresh + reconnect-refetch + echo-dedupe)
- [x] **2E** Board (dnd-kit + position) + quick-add + drawer (comments/activity/assignee/watchers/labels/due) + sprint view + completion bars + optimistic UI/toasts/skeletons + notification bell + deep-link; **verified in-browser**
- [x] API + WS tests (CRUD, RBAC, pagination round-trip, soft-delete, invite, notification IDOR, WS handshake); REDIS_URL + truncation order in harness — **34 tests** across tickets/ticket-detail/sprints/invites/notifications/realtime; harness `setup.ts` truncates in FK order, realtime.test sets `REDIS_URL`
- Dropped/deferred: `bulk-update` (deferred); dark mode/i18n/mobile/Cmd-K/Playwright → **Phase 2.5**

---

## Phase 2.1 — Gap closure → [plan](agentpm-plan/phases/phase-2.1-gap-closure.md)
**Status:** ✅ **COMPLETE** — all 11 items (A+B+C) implemented & committed (`3db2df1`); typecheck/build/35 API tests green. Closes gaps between the Phase 2 plan (drawer/board/UX/DoD) and what 2A–2E shipped. (Historically "2F"; renumbered to **2.1** as it patches Phase 2.)

_Group A — backend ready, UI wiring only:_
- [x] **A1** Sprint picker in the ticket drawer (associate ticket↔sprint via `updateTicket({sprintId})`)
- [x] **A2** Per-card JIRA-style status dropdown on the board (hover ⋯ menu; stopPropagation so it doesn't drag/open)
- [x] **A3** Delete/archive ticket from the drawer (confirm → `deleteTicket` → close + refetch)
- [x] **A4** Search/filter/sort bar on the board (q debounced + priority/type/assignee/sprint/sort; query key includes params)
- [x] **A5** Invite-member UI: new `/orgs/:slug/members` page — members list + create/copy invite link + list/revoke pending
- [x] **A6** Sprint↔tickets on the Sprints page (expand row → list tickets + remove + add-ticket picker)

_Group B — needs new backend + UI:_ ✅ **done (typecheck+build+35 tests; not browser-verified)**
- [x] **B7** Labels: `/api/labels` CRUD (org-scoped) + label-assignment via `PATCH /tickets/:id` (`labelIds` replace-set, cross-scope guard) + drawer picker (chips/add/create-with-color) + API test
- [x] **B8** @mention member picker in the comment box (trailing `@` → member autocomplete → `@[uuid]` token; renders back as `@Name`)

_Group C — polish / pre-existing:_
- [x] **C9** Hard-refresh → Landing: Keycloak `check-sso` + `public/silent-check-sso.html` _(needs browser check — silent iframe can be blocked by third-party-cookie rules)_
- [x] **C10** Within-column drag reordering — cards use `useSortable`+`SortableContext` (closestCorners); drop computes target column + insert index → fractional `positionBetween` neighbours; DragOverlay kept _(implemented; **reorder feel needs an in-browser check**)_
- [x] **C11** Drawer optimistic updates (scalar fields merge into the `['ticket']` cache instantly, rollback on error)

> **All 11 implemented** (A1–A6, B7, B8, C9, C10, C11) + extras: move-ticket-between-sprints, add-member-by-email on Members page, mention shows display name (not UUID), card hover-overlap fix. Verified by typecheck/build/35 API tests. **Browser-verify pending** — especially C9 (cookie-dependent), C10 (reorder feel), and the mention→notification chain.

---

## Phase 2.5 — UX Hardening → [plan](agentpm-plan/phases/phase-2.5-ux-hardening.md)
**Status:** ✅ **COMPLETE** — dark mode, i18n, mobile, Cmd-K, a11y + Playwright E2E scaffold. (E2E executes locally per docs; CI wiring → Phase 3.)
- [x] Dark mode (Tailwind `darkMode:'class'` + `.dark` CSS-var palette; `theme.ts` localStorage+OS pref, applied pre-render; toggle in Layout; retrofit Landing/Dashboard/OrgProjects to tokens; Toaster `theme=system`)
- [x] i18n (react-i18next + LanguageDetector, `en` baseline in `locales/en.json`, `lib/i18n.ts`; **all UI strings externalized** across every page/component incl. Phase-1; localStorage persistence)
- [x] Mobile-responsive board + drawer (board snap-scrolls, columns `85vw` on mobile; drawer already full-width sheet; Mouse+Touch(long-press 220ms)+Keyboard dnd sensors; header email hidden on small screens)
- [x] Cmd-K command palette (`cmdk` + `components/ui/command.tsx`; `CommandPalette` mounted in Layout, ⌘/Ctrl-K; quick-create ticket from query, jump to ticket by #/title, switch project/org; context from URL)
- [x] Playwright E2E scaffold (`playwright.config.ts` + `e2e/global-setup.ts` Keycloak login→storageState + `e2e/core-flow.spec.ts` create-org/project→add/open ticket→comment, + optional cross-user mention assertion via password-grant API; `test:e2e` script) — **runs locally** per docs (needs `pnpm exec playwright install` + seeded KC user + `docker compose up`); CI wiring → Phase 3. **a11y pass:** card keyboard activation (Enter/Space) + focus ring + aria-label; bell aria-label; radix dialogs trap/restore focus

---

## Phase 2.6 — UX Delight & Agent-First Polish → [draft](agentpm-plan/phases/phase-2.6-ux-delight.md)
**Status:** 🟢 **Slice 1 shipped (7 items)** — quick-win delight pass, web-only, typecheck/build green; **in-browser verification pending**. Remaining groups build selectively (not a gate). "Phase 5-dep" = UI now, action when the agent lands.

_A — Agent-first signatures:_
- [x] **A1** Ticket "readiness meter" (goal/AC/constraints fill → ring) — S _(ring on card + drawer; drawer now edits goal/constraints too)_
- → **A2, A3, A4 moved to [Phase 5.5](agentpm-plan/phases/phase-5.5-agent-first.md)** (parked; imply an actual agent, wire with Phase 5)

_B — Board:_
- [x] **B1** Live "ghost drag" via presence/WS — M _(ephemeral `ticket.drag` relay → faint ghost card w/ dragger avatar in target column — build+test verified)_
- [x] **B2** Column WIP-limit pulse — S _(IN_PROGRESS/IN_REVIEW limit 3; badge shows N/limit, pulses amber over — verified badge format)_
- [x] **B3** Swipe-to-advance (mobile) — S _(quick horizontal flick → next/prev status; composes with dnd touch listener — build-verified)_
- [x] **B4** Focus mode (`f`) — S _(dim non-mine cards; `f` key + header toggle)_
- [x] **B5** Time-decay card coloring (by `updatedAt`) — S _(left border darkens with age; fresh <2d = none — build+logic verified)_

_C — Drawer:_
- [x] **C1** Unified activity+comments "story" timeline — M _(third drawer tab, chronological merge — verified)_
- [x] **C2** Acceptance-criteria checklist → completion — M _(no backend — `- [ ]` task-list convention in AC; interactive checkboxes + N/total)_
- [x] **C3** In-editor slash commands — M _(`/status /assign /sprint /due /label` in the comment box — verified `/status done`)_
- [x] **C4** Relative time, exact on hover — S _(drawer comments/activity + notification bell)_

_D — Command palette:_
- [x] **D1** Full action surface (status/assign/sprint/label/theme) — M _(cmdk sub-pages on the open ticket + global theme toggle)_
- [x] **D2** Recent / frecency — S _(localStorage frecency; Recent tickets when query empty)_
- [x] **D3** Natural quick-create (`!high @user #sprint`) — M _(parser → priority/assignee/sprint; badges in the create item)_

_E — Notifications / presence / realtime:_
- [x] **E1** Ticket-level presence (who's on which ticket) — M _(ephemeral `ticket.viewing`→`ticket.presence`; pulsing viewer avatars on card + drawer — build+test verified)_
- [x] **E2** Toast → Undo (reuse rollback snapshot) — S _(board column moves + every drawer field patch)_
- [x] **E3** Notification grouping + "catch me up" — M _(no backend — client-side group by ticket "N updates", unread-since header)_

_F — Sprints / planning:_
- [x] **F1** Burndown sparkline — M _(no snapshot table — `GET /sprints/:id/burndown` reconstructs remaining-work-over-time from activity; inline SVG ideal-vs-actual on active/completed sprints)_
- [x] **F2** Drag tickets into a sprint — M _(Sprints page: backlog drop-zone + sprint cards droppable; dnd-kit → add/removeFromSprint)_
- [x] **F3** Velocity-aware capacity bar — S _(committed pts vs last completed velocity; amber overcommit — build+logic verified, shows when pts>0)_

_G — Delight / craft:_
- [x] **G1** "Done" confetti (reduced-motion aware) — S _(canvas burst, no dep; fires on move→DONE board + drawer)_
- [x] **G2** Per-org accent color + theme tristate + `t` — M _(migration `Organization.accentColor` + PATCH; accent → `--primary` (Layout); theme light/dark/system + `t`; accent picker on Members)_
- [x] **G3** Layout-matched skeletons — S _(`BoardSkeleton` mirrors columns/cards)_
- [x] **G4** Keyboard help overlay (`?`) — S _(radix dialog; ⌘K / f / ? / Enter / Esc)_

_H — Onboarding / empty states:_
- [x] **H1** Guided first ticket (name→goal→column) — S _(empty-board starter card → creates in Backlog + opens drawer — verified)_
- [x] **H2** Invite nudge on empty members — S _(team-of-one banner → create invite link — build+logic verified)_

> Suggested first slice (quick, no backend): **E2 Undo · A1 readiness · G1 confetti · G3 skeletons · G4 `?` · C4 relative time · B4 focus**. Detail/effort per item in the [draft](agentpm-plan/phases/phase-2.6-ux-delight.md).

---

## Phase 2.8 — Branding (PMAgent) → [plan](agentpm-plan/phases/phase-2.8-branding.md)
**Status:** ✅ **IMPLEMENTED (2026-06-26)** — product + Keycloak rebranded to **PMAgent** (camel-case). Web typecheck + build green; **35 API tests green** (host — the `node:20` container has no global `WebSocket`, so the 3 realtime tests only pass on Node ≥21/host). Keycloak login theme verified over HTTP (theme `pmagent.css` + `logo.svg` → 200; realm `displayName`/`loginTheme` set). Live pixel screenshot still worth a look.
- [x] Web: `index.html` title + `favicon.svg` · `en.json` `common.appName` + `invite.title` · Swagger `title` → **PMAgent**
- [x] Keycloak **Tier 2**: custom `pmagent` login theme (`infra/keycloak/themes/pmagent/login` — `theme.properties` + `pmagent.css` + wordmark `logo.svg`), mounted into the KC container; realm `displayName`/`displayNameHtml`=PMAgent + `loginTheme`=pmagent; client display names → PMAgent
- [x] E2E brand assertion updated (`/PMAgent/i`); web typecheck/build + 35 API tests green
- [x] favicon (`apps/web/public/favicon.svg`, PM monogram) + custom Keycloak login theme (logo + slate-900 brand)
- Decisions settled: wordmark **PMAgent**; Keycloak **Tier 2**; identifiers (`@agentpm/*`, realm/client ids, `agentpm.io`) intentionally unchanged.

---

## Phase 2.8.5 — Auth UX: in-app OAuth (no Keycloak login page) → [plan](agentpm-plan/phases/phase-2.8.5-auth-ux.md)
**Status:** 🟢 **frontend + IdP plumbing implemented (2026-06-26); social login pending OAuth creds.** In-app **Google/Microsoft/GitHub** buttons via `idpHint` (no Keycloak page) + email/password (**2.8.5b** → branded KC page). Web typecheck/build green; `keycloak-init` runs clean (exit 0): skips IdPs without creds, disables first-broker "Review Profile". Social round-trip verifiable once the OAuth apps are registered.
- [x] Frontend: `lib/auth.ts` `loginWith(idp)` (`keycloak.login({ idpHint })`); `Landing.tsx` Google/MS/GitHub buttons (`ProviderIcons`) + email/password (→ branded KC page) + PMAgent logo; i18n
- [x] Runtime IdP plumbing: `keycloak-init` extended (kcadm upsert google/microsoft/github from env, skip if no creds; disable first-broker "Review Profile" → seamless auto-create); `trustEmail`; `GOOGLE_/MICROSOFT_/GITHUB_CLIENT_*` in `.env`/`.env.example` (empty placeholders, never committed)
- [x] Email/password mechanism — **decided: 2.8.5b hybrid** (branded Keycloak page)
- [ ] Prereq (user): register OAuth apps (Google Cloud / Azure AD / GitHub) → drop client id+secret into `.env` → `docker compose up -d --force-recreate keycloak-init`. **Runbook:** [references/13-social-login-setup.md](agentpm-plan/references/13-social-login-setup.md)
- [ ] Verify the social round-trip in-browser on localhost with ≥1 real provider (blocked on creds)

---

## Phase 3 — Containerized Deployment + CI/CD → [plan](agentpm-plan/phases/phase-3-dev-deployment-cicd.md)
**Status:** ⬜ not started
- [ ] `docker-compose.prod.yml` + Caddy config + `Makefile` (`up-managed` / `up-selfhost`)
- [ ] Provision managed data (RDS + ElastiCache) + create `agentpm`/`keycloak` DBs
- [ ] Provision VM + DNS (`agentpm.io` / `api.` / `auth.`)
- [ ] Prod `.env` on VM (managed endpoints, locked perms)
- [ ] GitHub Actions CI (lint/typecheck/test) + CD (build/push images → migrate → `compose up -d`)
- [ ] Deploy to staging end-to-end + hardening checklist

---

## Phase 4 — Notifications & Channels → [plan](agentpm-plan/phases/phase-4-notifications-channels.md)
**Status:** ⬜ not started
- [ ] Email (SES) + notification worker + sprint digest cron
- [ ] WhatsApp + Slack two-way (post-MVP)

---

## Phase 5 — GitHub Integration + Code Agent → [plan](agentpm-plan/phases/phase-5-github-code-agent.md)
**Status:** ⬜ not started
- [ ] GitHub App + connect flow + webhook receiver
- [ ] Shared agent utils + repo reader + code generator + PR creator
- [ ] BullMQ queue + concurrency guard + worker service
- [ ] AgentAction logging + rollback + approval gate
- [ ] Frontend: assign agent, activity feed, approval UI, PR link
- [ ] Trial/billing guard on agent runs (cost control)
- [ ] Agent-first UI surfaces (A2/A3/A4) — land in **[Phase 5.5](agentpm-plan/phases/phase-5.5-agent-first.md)**

---

## Phase 5.5 — Agent-First Surfaces → [plan](agentpm-plan/phases/phase-5.5-agent-first.md)
**Status:** ⬜ **parked → lands with Phase 5** — agent-first UI whose *action* needs the Code/Spec agent. Renumbered from 2.7 (it's an agent phase, so it sits right after Phase 5). A1 (readiness ring) already shipped in 2.6.
- [ ] **A2** `@agent` first-class in mention/assignee pickers — M
- [ ] **A3** "Draft with agent" goal/AC/constraints skeleton — M
- [ ] **A4** Agent swimlane/badge on the board — S–M

---

## Phase 6 — Full Agent Suite + Autonomy → [plan](agentpm-plan/phases/phase-6-agent-suite-autonomy.md)
**Status:** ⬜ not started
- [ ] Spec / QA / Deploy / Observability agents (+ per-run container isolation)
- [ ] Autonomy dial (server-side enforcement; prod always human)

---

## Phase 7 — Autonomous Sprints → [plan](agentpm-plan/phases/phase-7-autonomous-sprints.md)
**Status:** ⬜ not started
- [ ] Sprint Planner Agent + multi-agent coordination + parallel workstreams + analytics

---

## Log (newest first)

| Date | Phase | Step / change | Commit |
|---|---|---|---|
| 2026-07-16 | P3.8.4 | **B5 — ticket creation inherits the ad-hoc context (BUG-5).** Audit of board create paths: `quickAdd`/`createFromDraft` already inherited the workstream tab, but `createFromTemplate` didn't — a template used on the Ad-hoc tab created a sprint-work ticket that landed in the backlog. Extracted a shared, unit-tested `workstreamForTab(wsTab)` in `lib/board.ts` and routed all three create paths through it (DRYs the duplicated ternary). Off-board paths (CommandPalette, empty-project first ticket) have no tab context and keep the server default. Browser-verified: quick-add on the Ad-hoc tab → NEW-12 created with workstream Ad-hoc, showing on the Ad-hoc board immediately; throwaway deleted. +2 unit tests; web 65→67, typecheck + build green. | cca60e7 |
| 2026-07-16 | P3.8.4 | **B6 — honest, stable milestone readiness labels (BUG-2 interim + UX-4).** Copy only (the date-window math is unchanged; durable fix = Milestones v2). Overview + Reports readiness now reads **"N of M tickets done"** (was "N/M ready"/"N/M done"), a `total===0` milestone shows **"No tickets due before this date"** instead of "0/0", and a `title` tooltip explains the window rule. i18n reworded/added on both `overview.*` and `reports.*`. Browser-verified on Overview (dated-tickets milestone → "1 of 4 tickets done"; before-all-tickets milestone → "No tickets due before this date"); throwaway milestone deleted. typecheck + web 65/65 + build green. *(Found a stray "tes" milestone not created by this work — left in place.)* | 04c069a |
| 2026-07-16 | P3.8.4 | **U3 — label overflow "+N" in the List (UX-3).** `ProjectList.tsx` title cell: after the first 3 label chips, a muted `+{n}` chip (with a `title` listing the remaining names) when a ticket has >3 labels — no label is hidden silently anymore. Board cards already wrap all labels, so no change there. Browser-verified on NEW-8 with 5 labels → List showed 3 chips + **"+2"**; fixtures removed (labels unassigned, org labels untouched). typecheck + web 65/65 + build green. | 5da3d1c |
| 2026-07-16 | P3.8.4 | **B4 — drag-and-drop into the BLOCKED column (BUG-4).** Reproduced: the drop logic is fine (dragging into a fully-visible Blocked works), but Blocked is the 5th of 6 columns so it's usually off-screen; dragging toward it made the card land one column short (In Review) because `closestCorners` snapped to the nearer visible column while horizontal auto-scroll lagged. Fix in `Board.tsx`: hybrid collision `boardCollision` = `pointerWithin` → `closestCorners` fallback (drop lands on the column under the pointer), plus `autoScroll={{ threshold: { x: 0.25, y: 0.2 } }}` to begin scrolling sooner. Browser-verified: the off-screen To Do→Blocked drag now lands in Blocked; visible drops unaffected; data restored. web 65/65, typecheck + build green. Non-drag fallback (per-card status dropdown) still available. | d304a65 |
| 2026-07-16 | P3.8.4 | **B3 — dependencies visible on the Timeline (owner O2).** Refined the finding: the API already returns edges touching unscheduled tickets (undated tickets are in `payload.items`); the defect was purely frontend — the chart got only `scheduled` items, so any edge whose other end was in the tray hit `if (!from||!to) return null` and vanished. Fix (frontend + API guard test, no service change): new pure `classifyEdge(edge, isScheduled)` in `lib/gantt.ts` (arrow when both ends scheduled · glyph on the scheduled bar when one end off-chart · none) + unit test; `GanttChart` takes a `ticketMeta` map and renders arrows for scheduled pairs + a destructive-token dot with a `<title>` naming the off-chart ticket (grouped per bar); `ProjectGantt` adds the tray-chip dot + a dependency legend. i18n `gantt.depBlockedBy/depBlocks/legendDependsOn/legendOffchart`. API guard test: a dep whose depends-on has no dates still appears in `edges`. Browser-verified (arrow NEW-3→NEW-2 · off-chart glyph on NEW-2 · tray dot on NEW-11 · legend) with throwaway fixtures, all removed. api 118→119, web 64→65, typecheck + build green. *(Flagged in passing: deleting a ticket leaves dangling dependency rows where it was the blocker — separate pre-existing bug, background task.)* | af2df3b |
| 2026-07-16 | P3.8.4 | **B2 — milestones discoverable on the Timeline (BUG-1).** Milestone diamonds already rendered but sat far off-screen for far-future dates with no cue. Added a **Milestones** chip strip under the Gantt toolbar (`ProjectGantt.tsx`): one chip per milestone (amber ◆ · name · date), click scrolls the chart to it (reuses the `scrollToToday` idiom); chips whose diamond is currently outside the horizontal viewport show a `←`/`→` arrow, driven by a new pure `milestoneViewport(milestones, rangeStartDay, scale, scrollLeft, clientWidth)` helper in `lib/gantt.ts` (unit-tested) + a scroll/resize listener on the chart container. i18n `gantt.milestonesLabel`. Browser-verified (chip appears · off-screen arrow · click scrolls · arrow clears in view) with a throwaway 15 Dec 2026 milestone, then deleted — data restored. web 63→64, typecheck + vite build green. | d6057a2 |
| 2026-07-16 | P3.8.4 | **B1 — ticket drawer opens in place on the Timeline (BUG-3 / owner O1).** Root cause: `openTicket` navigated to the `/board/ticket/:n` route (no gantt ticket route existed), so opening a ticket from the Timeline jumped to the Board. Fix mirrors the List/Board drawer idiom: new `/orgs/:slug/projects/:projectSlug/gantt/ticket/:number` route (`App.tsx`) → `ProjectGantt` reads `:number`, resolves the ticket from the loaded gantt payload, renders `<TicketDrawer>` in place (`onChanged` invalidates `['gantt', projectId]`); `openTicket`→`/gantt/ticket/:n`, `closeDrawer`→`/gantt`. Browser-verified (open over Timeline · close back to Timeline · cold deep-link) — web typecheck + vite build + 63/63 green; no test data changed. | 6784b6f |
| 2026-07-16 | P3.8.4 | **P0 — Phase 3.8.4 opened (beta-feedback polish & bug fixes).** First external beta test triaged: 19 findings (FINDINGS.docx) → requirements doc `~/Downloads/PMAgent-Beta-Requirements.docx` (v0.2, owner-reviewed) → step-by-step plan `agentpm-plan/phases/phase-3.8.4-beta-polish.md` (B1–B6 bugs, U1–U3 quick UX, V close-out; root causes pre-diagnosed in code with line anchors). Owner directives folded in: **O1** ticket from Timeline opens in the same slider, stay-on-screen open & close (revises BUG-3; root cause `ProjectGantt.tsx:81` navigates to the Board route — no `/gantt/ticket/:n` route) · **O2** show dependent tickets on the Timeline (display-only; edges exist but drop when an endpoint is unscheduled — `GanttChart.tsx:264-267`, `items={scheduled}`). Out of scope, queued: Milestones v2 (3.8.5 candidate), FEAT-1/2, ⛔DEC-1 sprint-board model, ⛔DEC-2 action-oriented Overview, DEF-1/2/3. | e5f1a59 |
| 2026-07-13 | BUGFIX/CONFIG | **Rate-limit fix — "Rate limit exceeded" during normal navigation (owner-reported, List page screenshot).** **Root cause (bug):** `@fastify/rate-limit` keys on `request.ip` (no `keyGenerator`), and Fastify was built **without `trustProxy`** — behind Caddy (`reverse_proxy api:3001`) every user's `request.ip` was Caddy's container IP, so the whole deployment shared ONE 100/min bucket (dev: browser + verify scripts shared the Docker gateway IP the same way). One List navigation fans out ~15 requests → normal clicking crossed the shared bucket. Also latent: all prod users collectively shared the AI routes' 10/min. **Fixes:** (1) `trustProxy: true` — `request.ip` = real client via X-Forwarded-For; **safe because docker-compose.prod.yml publishes only Caddy's 80/443** (api unreachable directly → header unspoofable); (2) global cap now **env-tunable `RATE_LIMIT_MAX` (default 400/min)** per real client — abuse backstop, documented in both `.env*.example`; prod needs no deploy change (code default applies); AI per-route limits (5–10/min) unchanged, now correctly per-client; (3) **`/health` + `/ready` exempt** (`allowList`) so monitors/healthchecks never eat budget or false-alarm; (4) **graceful client**: `request()` in `lib/api.ts` gives an idempotent **GET one silent retry after the server's `Retry-After`** (pure `retryAfterMs`, clamped 1–10s) — transient bursts self-heal instead of flashing errors; **mutations still fail fast** (never silently re-fire a write). **Rejected:** keying by JWT `sub` — the limiter runs before auth, and an unverified sub lets an attacker mint unlimited buckets (limiter bypass). +2 hermetic API tests (own server with `RATE_LIMIT_MAX=2`: separate XFF buckets — the pre-fix shared bucket would fail this; health exemption post-exhaustion) + 3 web unit tests (`retryAfterMs` clamp/fallback) — **api 116→118 · web 60→63**, typecheck green. **Live-verified on the dev stack:** client A 429s exactly at request #401 (cap honored), client B (different XFF) unaffected, `/health`/`/ready` 200 while A exhausted, A still 429 on API routes. | b5b4148 |
| 2026-07-13 | P3.8.3 | **S1 — draft a sprint goal with AI.** New endpoint `POST /api/ai/sprint-goal {sprintId}` through the seam: loads the sprint + up to 15 committed ticket titles, MEMBER role-gate, `generateLogged(…, 'sprintGoal', …)`. New registry entry in `ai.prompts.ts` (`sprintGoalZod {goal:1..300}` + `SPRINT_GOAL_SYSTEM` "one outcome-focused sentence ≤~25 words, don't list tickets" + `buildSprintGoalUser`); **`generateLogged` endpoint type widened `'draft'|'expand'|'summary' → PromptEndpoint`** so registry growth auto-covers telemetry; **PROMPT_VERSION 2→3** (A6 test assertion updated). FE (`Sprints.tsx` goal editor): `AIButton` "Draft goal with AI" → `AIThinkingIndicator` while generating → the goal **streams into the field** (readOnly + Save disabled while streaming), editable after, normal Save persists (zero AI auto-save); `aiErrorKey` copy + `AbortSignal` cancel. +2 hermetic API tests (happy path asserts title reached model + goal NOT persisted; 404 unknown sprint) — **api 114→116**, web 60, typecheck green. **Browser-verified live (Playwright, Nova Micro):** created a sprint, opened the goal editor, Draft-goal → busy/aria-busy + shimmer "Thinking" + readOnly-during-stream confirmed, goal typed into the field, Save persisted. i18n `ai.draftGoal`. | 5fe90a6 |
| 2026-07-12 | P3.8.1 | **A4 — model A/B benchmark + ₹/mo cost table (decision-support for ⛔A5).** Ran the A1 harness (`ai:eval --model`, 16 fixtures × runs 3 = 48 gen/model, prompt v2) across **four Nova models** on real Bedrock: `apac.amazon.nova-micro-v1:0` (baseline/live), `apac.amazon.nova-lite-v1:0`, `global.amazon.nova-2-lite-v1:0`, `apac.amazon.nova-pro-v1:0`. **Claude skipped** — every Anthropic id (incl. Claude Haiku 4.5 `global.anthropic.claude-haiku-4-5-*`) returns `ResourceNotFoundException: Model use case details have not been submitted for this account` (one-time Anthropic use-case form not done); per plan **Nova Pro is the no-form high-tier fallback**. Also confirmed `list-inference-profiles` now works (the 2026-07-10 policy gap is closed). **Findings:** all 4 Nova models 100% valid-first-try/0 retries (schema reliability is a non-differentiator); **Nova Lite is the value upgrade** — expand AC-testability 35%→77%, draft 22%→31%, ~1.7× cost (~₹43/mo vs Micro ₹26/mo at 7k gen/mo); **Nova Pro** best draft-AC (75%) but ~21× cost (~₹553/mo), not worth it; **Nova 2 Lite** worst value (slowest 2.4s, ~1.7× tokens, no quality edge, price unconfirmed). Report body kept as the Micro (production) run + appended **§A4 A/B** section with both comparison tables, the ₹/mo assumption, and a suggested A5 shortlist (stay Micro for cost, else flip `BEDROCK_MODEL_ID` to Nova Lite). **No product code changed** (temp probe script removed); only `release-doc/ai-eval-report.md`. Real-token spend ~₹5 total. | (docs) |
| 2026-07-12 | P3.8.1 | **B4 — auto-fill per-field review (confirm AFTER generation) + B5 polish sweep + owner-requested modern loader.** **B4 (behavior inversion):** dropped the pre-call `window.confirm(ai.overwriteConfirm)`; auto-fill now generates immediately, then renders a **per-field current-vs-proposed review** (`AutoFillReview` in `TicketDrawer.tsx`) for description/goal/AC/constraints. Pure logic in new **`lib/aiExpandReview.ts`**: `buildReviewFields` (only fields the AI actually proposed — empty proposals skipped; **empty target → pre-accepted, filled target → conflict badge + default keep-current**), `composeAccepted` (accepted proposals win, rest keep current; never blanks a field from an empty proposal), `defaultAccepts`/`setAllAccepts` (Accept-all / Keep-all). Proposed text **streams in sequentially** in the review (`sliceSequential`, keyed on `draft` so a websocket ticket-sync re-render can't restart it); Apply lands accepted values in the edit fields, normal **Save** persists (zero AI auto-save unchanged). Focus moves to the review block, `aria-live` announce, Esc discards, destructive `accent` on conflict toggles. **Owner-requested loader redesign (B5):** replaced the result-shaped **skeletons** on all three surfaces with a shared **`AIThinkingIndicator`** — sparkle + gradient-shimmered stage word (**Thinking → Cooking → Finishing**, rising in on change) + bouncing dots + Cancel; CSS `ai-shimmer`/`ai-dot` in `index.css`, solid/instant under `prefers-reduced-motion`. Stage i18n keys renamed reading/drafting/almost → thinking/cooking/finishing. **B5 i18n audit:** removed dead `ai.overwriteConfirm` (B4) + `ai.apply` (obsoleted); `ai.modelLoading` kept (health gating). +6 tests (`aiExpandReview`: build/compose/accept-all/keep-all/thin-input) — **web 54→60**, typecheck green. **Browser-verified live (Playwright, e2e user, real Nova Micro):** thinking loader shows during generation (`ai-shimmer-text`=Thinking, aria-busy); empty ticket → 4 review rows all pre-accepted, 0 overwrite badges; filled ticket → all 4 default keep-current, 4 "Replaces existing" badges + 4 CURRENT blocks; Accept-all → all checked, Keep-all → all unchecked; Apply filled the 4 fields (268/121/381/63 chars) in edit mode; no upfront confirm ever fired. Test ticket left in the e2e project. | 4d2fb53 |
| 2026-07-12 | P3.8.1 | **B3 — editable draft preview + regenerate + per-bullet AC; owner follow-up to B2 — auto-fill fields stream in too.** **Auto-fill streaming (TicketDrawer):** on generation success the four spec fields type in sequentially (description → goal → AC → constraints) via new pure `sliceSequential`/`countSegments` in `aiReveal.ts` (~40ms ticks); fields are **readOnly + Save disabled while streaming** so a partial value can't be edited into or persisted; the stream cancels if the drawer switches tickets; instant under reduced motion. **B3 (Column `DraftPreview`):** after the B2 reveal finishes the card becomes editable — title `Input`, description `Textarea`, priority native select over `PRIORITIES`, **per-AC-bullet checkboxes** (all checked by default, unchecked bullets struck through); Create submits `composeEditedDraft` (new pure `lib/aiDraftEdit.ts`: edited fields + unchecked AC dropped; blanked title falls back to the generated one); **Regenerate** re-runs with the same notes, `window.confirm`s **only when `isDraftEdited`** (house confirm pattern), replaces the draft and replays the reveal; Create still works mid-reveal. `Board.tsx`'s `- `-join contract unchanged. +6 tests (compose/dirty ×4, sliceSequential ×2) — **web 48→54**, typecheck green. **Browser-verified live (Playwright, e2e user, real Nova Micro):** pristine regenerate → 0 confirms; dirty regenerate → exactly 1 confirm ("Regenerate and replace your edits…"), edits kept on dismiss; created card carries the edited title and the ticket's AC dropped the unchecked bullet; auto-fill sampled at 120ms — skeleton ~1.1s then fields fill 189/84/265/73 chars in strict sequence over ~1.5s, drawer closed unsaved. i18n +4 keys. | cc8c1a0 |
| 2026-07-12 | P3.8.1 | **B2 — perceived latency: skeleton + staged hint + cancel + staged "streaming" reveal (owner-requested UI streaming; no real token streaming, per the B2 decision).** `lib/api.ts` threads an optional `AbortSignal` through `request()`→`authedFetch()`→`fetch` (exposed on the three AI calls only); `aiErrorKey()` now returns **null** for `AbortError` so a user cancel resets silently — keyed on `err.name`, since **DOMException does not extend Error** (an `instanceof Error` guard would have missed real fetch aborts; caught by the new unit test). New **`lib/aiReveal.ts`**: `useTextReveal` (word-by-word, whitespace-preserving `segmentText` so newlines survive), `useListReveal` (staggered list items), `useStagedHint` ("Reading context…"→1.5s→"Writing…"→4s→"Almost there…", pure `stageAt`); all instant under `prefers-reduced-motion` (also flipped `ui/skeleton.tsx` to `motion-safe:animate-pulse`). All three surfaces: result-shaped skeleton + hint + **Cancel** (draft: Esc in composer too; auto-fill: panel Esc + the Cancel button aborts while in flight; summary: `cancelQueries` + react-query signal), `aria-busy`, sr-only `aria-live` start/done announcements (staged hint deliberately not live). Draft preview & summary then **stream in**: title/headline types word-by-word → description → AC/bullets/risks one-by-one (extracted `DraftPreview`/`SummaryReveal`; Create works mid-reveal — the validated draft is already complete). Auto-fill fills fields instantly (animating editable textareas would fight the cursor). Dead `ai.generatingHint` key removed. **web 43→48** (abort-null, `segmentText` round-trip/prefix fidelity, `stageAt` boundaries), typecheck green. **Browser-verified on live Nova Micro** (instrumented sampling): draft — skeleton+hint+Cancel 0.1–1.2s, preview streams 77→386 chars over ~1.7s; summary — skeleton ~1.1s, headline 43→80 chars then bullets/risks 84→509; auto-fill — spec-block shimmer then 4 fields fill, nothing auto-saved; **Esc-cancel → idle, zero error copy** (Playwright). Test orgs left in the e2e account (b2-verify-*, same as the a11y-* artifacts). | d625133 |
| 2026-07-11 | P3.8.1 | **Browser verification of B1+A1–A3+A6 on real Nova Micro (full dev stack) + fixed a pre-existing draft bug found in the process.** Drove all three AI flows end-to-end through the UI against live Bedrock: **project-summary** (grounded digest, no hallucinated names/dates, risks calibrated to the real blocker), **auto-fill/expand** (testable AC "chronological order / last five messages", clean goal + PII constraint), **draft-ticket** (well-titled preview matching the project's naming register, 5 testable AC). All three emitted the **A6 telemetry line** in `docker compose logs api` (`evt:ai.generate`, endpoint/model/promptVersion:2/attempts:1/ms/tokens/outcome:ok; draft in-tokens 770 confirm A3 enrichment is flowing). **B1** verified live by forcing a 504 (recreated api with `AI_TIMEOUT_MS=1`): the draft showed the distinct *"The AI took too long. Usually transient — try again."* + Retry, not the old generic copy. **Bug found & fixed (pre-existing, not from 3.8.1):** the board quick-add composer's `onBlur={submit}` fired when clicking "Draft with AI", creating a raw ticket and tearing down the composer before `runDraft` ran — so Draft-with-AI never worked by mouse. Fix: guard the blur with a `composerRef` so only focus leaving the composer submits ([Column.tsx](sourcecode/apps/web/src/components/board/Column.tsx)). Re-verified: Draft with AI now shows the preview correctly. web 43/43, typecheck green. (Test artifacts NEW-9/NEW-10 archived afterward.) | c82f8da |
| 2026-07-11 | P3.8.1 | **A6 — generation telemetry line (prod evidence for the A5 model decision).** Promoted `model` + `generateDetailed` onto the `AIProvider` interface; a route helper `generateLogged(log, endpoint, provider, opts)` runs every generation through the seam and emits exactly **one structured pino line**: `{evt:'ai.generate', endpoint, model, promptVersion, attempts, ms, outcome, inputTokens?, outputTokens?}` — `outcome` = `ok`/`retry_ok` on success, `bad_output`/`timeout`/`unavailable`/`error` from the mapped `ApiError` code. No request-body logging; no new infra. All three routes wired through it. Hermetic test asserts the happy path emits exactly one `ok` line — captured via a **test-only `buildServer({loggerStream})` seam** (pino writes to fd 1 directly, so a real Writable stream is the reliable capture, not `process.stdout.write`). **api 113→114.** DoD note: line shape is exactly what reaches `docker compose logs api`. | b962dc2 |
| 2026-07-11 | P3.8.1 | **A3 — context enrichment (the biggest quality lever), measured through A1.** Unified the user-text assembly into shared `buildDraftUser`/`buildExpandUser`/`buildSummaryUser` in `ai.prompts.ts` (route AND harness call the same builders, so the eval measures production's exact text). **draft** now sends project name + up to 10 recent ticket titles (naming-style anchor) + the org's label vocabulary (`ticket.findMany take:10 orderBy updatedAt` + `label.findMany` — enrichment block capped < ~1.5k chars); **expand** sends parent title + up to 5 sibling titles (same-parent if a subtask, else nearest-in-project); **summary** sends the active sprint's `goal` (queried directly — `OverviewActiveSprint` doesn't carry it). Same org-role gate covers every added read; **titles/labels/goal only — never other tickets' descriptions** (PII posture unchanged). Harness fixtures gained a synthetic `enrichment` block (+ per-fixture `sprintGoal`). **Measured effect (Nova Micro, 48 gen):** valid-first-try went to **100% across all 16 fixtures with zero retries** (enrichment erased v2's flaky-retry noise); invention fell on thin inputs (terse 93→77, rambling 79→69, bug 53→41); +~2k input tokens total (35.3k), well within cap. +4 hermetic tests (draft titles/labels/name in payload; expand siblings; summary sprint goal; pure builder char-cap) — **api 109→113**. Report + change log in `release-doc/ai-eval-report.md`. | b7b3803 |
| 2026-07-11 | P3.8.1 | **A2 — prompt tightening per endpoint, measured through the A1 harness (PROMPT_VERSION 1→2).** Extended `GenerateOptions` with optional `temperature?`/`maxTokens?` (defaults preserve 3.8's 0.2/unset), threaded through `BedrockProvider.converse()`'s `inferenceConfig`; per-endpoint knobs live in the `PROMPTS` registry and the route spreads them (`...PROMPTS.draft`). **Measurement drove the decisions:** tried one **few-shot exemplar per endpoint** → on Nova Micro it cost **+23% input tokens (30.7k→37.8k) for zero scorecard gain + two regressions** (an added expand retry; empty-project summary dropped its bullets) → **DROPPED** (confirms "small models degrade with long prompts"). **Kept (v2, ~token-neutral 33.2k in):** summary now runs cooler (`temperature 0.1` vs draft/expand 0.2), `maxTokens` caps 400/500/350 as anti-rambling rails; directive tightening (don't-echo-verbatim, explicit anti-thin-input, verb-led measurable AC). Clearest measured win: **empty-project summary risks 3→1.3** (v1 over-flagged risks on a data-less project), bullets restored to ≥3; schema compliance stayed ~100% first-try. +2 hermetic tests asserting the `inferenceConfig` passthrough (draft 0.2/400, summary 0.1/350) — **api 107→109**. Delta recorded in `release-doc/ai-eval-report.md` (Prompt change log). AC-testability heuristic noted as noisy at 3 runs — directional only. | 8599723 |
| 2026-07-11 | P3.8.1 | **A1 — prompt extraction + eval fixtures + offline harness + baseline scorecard.** **A1a (mechanical):** moved the 3 system prompts + 3 JSON schemas + 3 zod shapes (+ `PRIORITIES`) verbatim out of `routes/ai.ts` into new `src/services/ai.prompts.ts` with `PROMPT_VERSION=1` and a `PROMPTS` registry; route now imports them (request-body validators + `cap` context assembly stay in the route). Exported a `createBedrockProvider(modelId?)` factory + a `generateDetailed()` on `BedrockProvider` that returns `{value, meta:{attempts, ms, inputTokens?, outputTokens?}}` (converse now also surfaces `output.usage`); `generate()` delegates to it so the request path is behavior-identical. **api still 107/107**, typecheck green — extraction is import-shuffling only. **A1b:** `scripts/ai-eval-fixtures.json` — 8 draft (terse/rambling/typo'd/bug/feature/chore/urgent/thin), 5 expand (title-only→rich, thin-desc, vague), 3 summary (empty/healthy/blocker-heavy) — all synthetic, no real data. **A1c:** `scripts/ai-eval.ts` (`pnpm --filter @agentpm/api ai:eval`, `--endpoint/--model/--runs`) — self-loads `sourcecode/.env`, hard-exits if AI unset, calls `generateDetailed()` **directly** (no HTTP/Keycloak), scores schema-valid-first-try / retry / AC count+testability / title words / invention smell / latency / tokens, prints an up-front cost estimate, writes `release-doc/ai-eval-report.md`. **Deliberately NOT a vitest test / not in CI** (spends real tokens). **Baseline (Nova Micro, 48 real generations, ~₹0.15):** 100% schema-valid on first try, 0 retries across all 16 fixtures; AC-testability low (0–62%) and invention high on thin inputs — the two levers A2 will target. | 8479e60 |
| 2026-07-11 | P3.8.1 | **B1 — truthful per-code AI error copy (ships alone, first).** New pure helper `aiErrorKey(err)` in `lib/useAIHealth.ts` maps a caught failure to an i18n key: `status===429` FIRST (rate-limit has no `code` — from `@fastify/rate-limit`, not `ApiError`), then `code` → `AI_UNAVAILABLE`/`AI_TIMEOUT`/`AI_BAD_OUTPUT`, else `ai.failed`. Added `ai.error.{unavailable,timeout,badOutput,rateLimit}` copy to `en.json`. Wired all three call sites: `Column.tsx` (draft) + `TicketDrawer.tsx` (auto-fill) replaced their `catch {}`→generic with `catch (e){ set…Error(t(aiErrorKey(e))) }`; `ProjectOverview.tsx` maps `summary.error` through it. On `AI_UNAVAILABLE` all three also `qc.invalidateQueries(['ai-health'])` so buttons re-gate without waiting the 60s staleTime (drawer/column inline; overview via `useEffect`). +6 `aiErrorKey` unit tests (one per branch). **web 37→43**, api untouched; typecheck + lint green. Retry affordance unchanged on every error. | a75d947 |
| 2026-07-11 | P3.8.1 | **Plan verification pass (max effort) — every claim audited against code; 4 defects fixed; plan is now handoff-ready.** Read all touched files end-to-end (ai.service/ai routes/api client/Column/TicketDrawer/ProjectOverview/BetaBadge/useAIHealth/skeleton/error handler/schema/e2e). **Defects found & fixed in the plan:** (1) *sequencing bug* — the A1 harness needs the prompts importable, but they're module-local consts in `routes/ai.ts`; A1 now starts with a mechanical extraction to `services/ai.prompts.ts` (+ `PROMPT_VERSION`), and `BedrockProvider` must be exported (it isn't today); (2) *missing plumbing* — `lib/api.ts` `request()`/`authedFetch()` accept no `AbortSignal`, so B2's cancel explicitly threads an optional signal through; (3) *429 shape gotcha* — rate-limit responses come from `@fastify/rate-limit`, not `ApiError`, so the body has **no `code`** → `aiErrorKey()` must key on `status===429` before `code` (would have silently fallen to generic copy); (4) *B4 behavior misread* — the overwrite `window.confirm` fires **before** generation today (`TicketDrawer.tsx:106`), so B4 is an inversion (generate → per-field review), not a swap of the confirm UI. **Additions:** A6 structured telemetry line per generation (endpoint/model/promptVersion/attempts/ms/outcome — prod evidence for the A5 model decision); B1 also invalidates the `['ai-health']` query on 503 so buttons re-gate; B2 handles `AbortError` as silent (not an error state) + `aria-busy`/reduced-motion; B5 flags `ai.overwriteConfirm` as dead post-B4 and keeps `ai.modelLoading` (still used by health gating); A4 notes `list-inference-profiles` returned empty for the dev IAM user (use `get-inference-profile` per candidate or widen the policy). **New §Handoff notes** (env/container/test/no-push/house rules) so a fresh Opus 4.8 high session can implement without re-deriving context; all Ground-truth anchors now carry verified `file:line` refs. Est. exit refreshed: api ≈111–112 · web ≈47–48. Plan only — no product code touched. | — |
| 2026-07-11 | P3.8.1 | **Phase spec — AI polish (output quality + UX) + full auto-populate catalog; plan only, nothing implemented.** Owner picked two polish axes; new `agentpm-plan/phases/phase-3.8.1-ai-polish.md` (BYOK renumbered → 3.8.2). **Part A quality (eval-first):** A1 manual eval harness `ai-eval.ts` + synthetic fixtures (~16 cases) scoring schema-validity/retry-rate/AC-testability/invented-requirements/latency/tokens → baseline report; A2 prompt tightening (one few-shot exemplar, anti-thin-input rule, per-endpoint temperature/maxTokens) kept only if the scorecard moves; A3 context enrichment (draft gets project name + ~10 ticket titles + label vocabulary; expand gets parent/sibling titles; summary gets sprint goal); A4 model A/B via `--model` flag (Nova Micro vs Nova 2 Lite vs Claude Haiku if the self-declaration form is done) + ₹/mo table; ⛔ A5 owner picks the prod model (pure `BEDROCK_MODEL_ID` flip). **Part B UX:** B1 per-code error copy — verified today all three call sites swallow the typed `ApiError.code` (`catch {}` → generic `ai.failed`) though `lib/api.ts` already delivers it; shared `aiErrorKey()` maps 503/504/502/429; B2 skeleton-shaped loading + staged hint + `AbortController` cancel (**decision: no real token streaming** — forced-tool JSON must arrive complete + validate before display; revisit only if latency >5s); B3 draft preview becomes editable + per-AC-bullet checkboxes + regenerate-with-dirty-confirm; B4 auto-fill overwrite-confirm → per-field diff/accept (empty fields pre-accepted, conflicts default keep-current); B5 i18n/a11y/browser sweep. **Part C — researched catalog of 18 further AI auto-populate surfaces** across ticket (subtask breakdown T1, AC-regen, title-sharpen, points-suggest, label-suggest, priority-sanity, duplicate-detect via the already-shipped pgvector image, comment TL;DR), sprint (goal draft S1, retro digest, planning assist), project (reports narrative, release notes, milestone risk, project brief), cross-cutting (NL quick-add X1 on the R9 token system, CSV mapping, NL search→filters, standup draft, template generator, notification digest) — each rated effort S/M/L; ⛔ C0 owner triages (suggested shortlist T1·S1·X1). Sequencing: B1 → A1–A3 → B2–B5 → A4 → ⛔A5 → ⛔C0; est. exit api ≈110 · web ≈47. | — |
| 2026-07-11 | P3.8 | **Part E — AI deployed to prod; PHASE 3.8 COMPLETE.** Chose the automated **GitHub-secrets → `.env.prod`** credential path (Option C) over the plan's EC2 instance role. `deploy.yml` deploy job reworked: adds `actions/checkout` + an **`appleboy/scp-action` step that syncs `docker-compose.yml`/`docker-compose.prod.yml` to the box** (the pipeline previously shipped images only — the box ran stale compose with no AI/AWS env passthrough, so `BEDROCK_MODEL_ID` came back empty and the container never received creds), then an idempotent `set_env` upsert writes `AI_PROVIDER`/`BEDROCK_MODEL_ID`/`AWS_REGION`/`AI_TIMEOUT_MS` + `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` (from repo secrets, only if non-empty) into `.env.prod` (`chmod 600`) before `up -d`. Secrets never touch git or images. Deployed on merge to `main`; prod `ai/health` green (`{enabled,reachable,modelReady,provider:bedrock}`); all three AI features live on `pmagent.sanjaykumarp.info`. **Incident during setup:** real AWS creds were mistakenly put in the tracked `.env.prod.example` (not the gitignored `.env.prod`) and committed locally — **GitHub push-protection blocked the push** (key never reached the remote); resolved by dropping the two offending local commits (`dev` reset to `origin/main`, which already carried all 3.8 code via PR #20). **Deferred hardening:** instance role (drop the key), $5 budget alert, key rotation. | 9a38701 |
| 2026-07-10 | P3.8 | **Deploy-prep (E-path) — AI creds on the box via a scoped dev key.** Owner wants AI live on the dev deployment now, refine later (instance role E1 deferred). Added `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` passthrough to the **prod** compose api service (`docker-compose.prod.yml` — base already passes `AI_PROVIDER`/`BEDROCK_MODEL_ID`/`AWS_REGION`/`AI_TIMEOUT_MS`; the AWS keys were previously only in the dev override, which prod doesn't load) + documented an AI block in `.env.prod.example` (empty `AI_PROVIDER` = AI off; set `bedrock` + the scoped key to enable; rotate/remove once the instance role lands). `docker compose -f docker-compose.yml -f docker-compose.prod.yml config` validates — api env now carries the AI + AWS vars. `@aws-sdk/*` confirmed in `dependencies` so the CI prod image includes them. **Deploy path:** merge local `dev` (13 3.8 commits) → `main` triggers the pipeline; dev↔main diverged (PR-based history) with **one PROGRESS.md merge conflict** (docs only; `index.ts` rate-limit-fix dup auto-merges). Box step: add the AI block to `.env.prod`, `chmod 600`. **Not pushed — awaiting owner go.** | — |
| 2026-07-10 | P3.8 | **D5 prompt-tune — force non-empty acceptanceCriteria.** Expand-ticket returned `acceptanceCriteria: []` on thin tickets. Prompt wording ("never return an empty list") + JSON-schema `minItems: 2` alone did **not** move Nova Micro (verified: 2/2 live runs still empty). Real fix: **`z.array(z.string()).min(1)` on the AC zod in both `draftTicketZod` and `expandTicketZod`** — an empty list fails validation, firing the seam's existing one corrective re-prompt with an explicit "must contain at least 1 element" message (the directive nudge small models obey). New hermetic test: expand returns `[]` once → retry → 2 AC (asserts exactly 2 Converse calls). **api 106 → 107** (ai.test.ts 17→18), full host suite green. Live re-verify: 3/3 runs returned 5 well-formed AC bullets (~2–2.6s; the retry costs ~1 extra round-trip). Owner picked "one tuning pass, stay on Nova Micro" over the Claude-Haiku flip. Test ticket cleaned up; temporary `agentpm-web` direct-grant reverted. Local on `dev`. | — |
| 2026-07-10 | P3.8 | **D5 — live verify against real Nova Micro (automatable side done).** AWS dev checklist complete: IAM user `pmagent-dev-bedrock` (creds in `sourcecode/.env`, `AI_PROVIDER=bedrock`); `aws sts get-caller-identity` + `get-inference-profile apac.amazon.nova-micro-v1:0` → **ACTIVE**; a forced-tool Converse smoke call returned schema-valid JSON (`stopReason: tool_use`, ~1.6s). Restarted api (loads live `.env`) and drove **all three endpoints end-to-end through the full stack** (real Keycloak token via a temporary `agentpm-web` direct-grant enable, **reverted after**; `list-inference-profiles` came back empty but is not on the app's path): **health** green `{enabled:true,reachable:true,modelReady:true,provider:bedrock}`; **draft-ticket 1.25s** → valid title/desc/6 AC bullets/`MEDIUM`; **project-summary 1.05s** → valid headline/3 bullets/risks (empty project); **expand-ticket 1.67s** → valid desc/goal/constraints **but `acceptanceCriteria: []`** (prompt asks 2–6 — prompt-tune candidate). Two throwaway tickets created for the expand test were deleted (archived) after; no code changed (api 106 · web 37 unaffected). **⛔ Owner quality judgment pending** (accept Nova Micro / prompt-tune expand / flip to Claude Haiku) → then Part E (E1 owner: prod instance-role IAM + IMDSv2 hop 2 + $5 budget; E2 ship). Local on `dev`. | — |
| 2026-07-09 | P3.8 | **D3+D4 — local-model surface removed + copy.** Deleted the `ollama` compose service + `ollama_models` volume; api env passthrough now `AI_PROVIDER`/`BEDROCK_MODEL_ID`/`AWS_REGION`/`AI_TIMEOUT_MS` (base compose) + dev-only `AWS_ACCESS_KEY_ID`/`SECRET` (override — prod uses the instance role, and empty strings are skipped by the SDK's credential chain); `.env.example` + local `.env` swapped to the Bedrock block (keys empty = AI disabled). **FEATURES.md**: "AI drafting (self-hosted)" → "AI drafting" — removed the "your text never leaves the server" claim, added a plain-language privacy note (cloud provider processes the text; don't paste secrets). i18n: `generatingHint` 10–20s → "usually just a few seconds", `poweredBy` drops "(self-hosted)"; web health fixture → `bedrock`. `git grep -i ollama` over tracked code/config: **zero hits**. Compose config validates; api container rebuilt with the new deps. **Full suite: api 106 · web 37, typecheck + vite build green** (one transient api-test flake in a `--force` run did not reproduce across two consecutive full green runs). D1–D4 ✅ → D5 awaits the owner's AWS checklist. | — |
| 2026-07-09 | P3.8 | **D1+D2 — `BedrockProvider` behind the AI seam + hermetic test swap.** `config.ts`: `OLLAMA_*` → `AI_PROVIDER` (''=disabled \| 'bedrock') + `BEDROCK_MODEL_ID` (default `apac.amazon.nova-micro-v1:0`) + `AWS_REGION` (ap-south-1); `AI_TIMEOUT_MS` default 120s→30s. `ai.service.ts`: `OllamaProvider` replaced by `BedrockProvider` — **Converse API with a single forced tool (`emit_result`) whose `inputSchema` is the endpoint's JSON schema**, draft read from `toolUse.input`; per-request `AbortSignal.timeout`; errors mapped to the seam's codes (`ThrottlingException`→503, timeout→504, invalid-args-after-one-corrective-retry→502); `health()` = free control-plane `GetInferenceProfile` (creds+access check, no tokens spent) → same `{enabled,reachable,modelReady,provider}` shape, **frontend untouched**. Credentials via the default AWS chain (dev keys / prod instance role — never in code). Deps: `@aws-sdk/client-bedrock-runtime` + `client-bedrock`. Tests: AWS SDK mocked at module level via `vi.hoisted`+`vi.mock` — no test touches AWS; cases: disabled/ready/profile-missing/unreachable health, happy path per endpoint (asserts no ticket persisted + exactly 1 Converse call), corrective retry, 502 after two bad, **new** throttle→503 + timeout→504, 400/403/404 guards assert the model is never called. **api 104 → 106.** | — |
| 2026-07-09 | P3.8 | **Strategy pivot: self-hosted Ollama → Amazon Bedrock (Nova Micro first); spec rewritten.** Owner decision after live Bedrock research (models reachable from ap-south-1 + pricing): **v1 inference = Amazon Nova Micro** via the APAC cross-region profile (`apac.amazon.nova-micro-v1:0`, verify at dev time) — ~₹10–40/mo at expected volume vs the old plan's **+₹1,050–1,250/mo EC2 upsize**, with a **pure env-var escape hatch to Claude Haiku 4.5/Sonnet** (`global.*` profiles from Mumbai, source-region pricing) if Nova Micro quality disappoints. Local-model tier **dropped** (owner accepts data leaving the product for MVP): old C1 compose service + `OllamaProvider` + `OLLAMA_*` env slated for removal (D3); C2/C3 (EC2 resize + runbook) **cancelled — never executed**. Plan file **renamed** `phase-3.8-local-ai-tickets.md` → **`phase-3.8-ai-tickets.md`** and fully rewritten: Part D (D1 `BedrockProvider` via Converse `toolConfig`-forced JSON + config swap · D2 hermetic AWS-SDK-mocked tests · D3 Ollama removal incl. FEATURES.md "self-hosted" wording · D4 copy · D5 live verify + prompt-tune) + Part E (deploy; prod auth = EC2 **instance role**, dev = scoped IAM-user key in `.env`) + a **🧑 AWS owner checklist** (model access incl. Claude form for later, profile-ID verify, `pmagent-bedrock-invoke` IAM policy, instance role + dev key, **$5 budget alert**, no SG/port changes). Every step tagged 🤖 (Claude Opus 4.8-executable) vs 🧑 (owner). Parts A/B remain permanent — endpoints, prompts, buttons, and the `AIProvider` seam are provider-agnostic by design; the pivot touches only the provider impl + infra. Docs-only commit (no code yet); background local-Ollama processes stopped, partial model download abandoned. | — |
| 2026-07-09 | P3.8 | **B1–B4 — the three AI buttons wired live (code-complete; live browser-verify pending a model).** Brings the 3.7 R10 inert affordances to life through the `AIProvider` seam. **B1:** `lib/api.ts` gains `aiHealth`/`aiDraftTicket`/`aiExpandTicket`/`aiProjectSummary` + `AI*` types; new `lib/useAIHealth.ts` (react-query, staleTime 60s) + pure `aiButtonState()` gate; new live **`AIButton`** in `BetaBadge.tsx` (Sparkles/Loader2, enabled only when health green, else disabled-with-reason tooltip). **B2** (`board/Column.tsx` + `Board.tsx`): *Draft with AI* uses the composer text as notes → draft **preview card** (title/description/AC bullets/priority) with **Create**/**Discard**; Create composes into the existing `createTicket` (AC array joined as `- ` bullets), never auto-creates; inline error + retry. **B3** (`TicketDrawer.tsx`): *Auto-fill from prompt* → optional steer input + generate, fills the editable spec fields (desc/goal/AC/constraints) in edit mode for review, **confirm-overwrite** when fields already have content, saved via the normal Save. **B4** (`ProjectOverview.tsx`): *Generate summary* → on-demand `{headline, bullets, risks}` rendered as typography, **Regenerate**, cached in react-query `['ai-summary', projectId]` (ephemeral, never persisted). New `ai.*` i18n namespace; **FEATURES.md** "AI drafting (self-hosted)" section (+ date bump). New `useAIHealth.test.ts` (5 tests, all `aiButtonState` branches). **web 32 → 37**, typecheck + vite build + full `turbo lint typecheck test` green (api 104 · web 37). **⚠️ Not yet browser-verified against a live model** — that + 1–2 prompt-tuning iterations (owner judges output) is the remaining Part-B exit step; needs a local Ollama (host vs container TBD) or waits for prod C2/C3. Then **⛔ owner checkpoint before C2/C3**. Local on `dev`. | — |
| 2026-07-09 | CI | **Fix: hermetic rate-limiting in tests (unblocks the dev→main deploy pipeline).** The `dev`→`main` merge pipeline's `pnpm turbo test` was failing with **86/104 api tests red** — every request (incl. `GET /health`) returned **429**, cascading into `Cannot read properties of undefined` in `makeOrg`/`provision`. Root cause: 3.7.4 D2 made rate-limiting **Redis-backed**, and CI sets `REDIS_URL` (the realtime/event-bus tests need it). The Redis limiter keys by client IP, so all test requests (from `127.0.0.1`) shared **one global 100/min budget across the entire suite** → exhausted mid-run → 429 storm. Locally the vitest env leaves `REDIS_URL` unset, so each `buildServer()` got an isolated in-memory counter and the suite passed — masking the bug (D2's comment even *claimed* tests were hermetic "without REDIS_URL", but CI provides it). **Fix** (`index.ts`): gate the Redis rate-limit store on `NODE_ENV !== 'test'` (not on `REDIS_URL`), so tests always use the per-app in-memory store and stay isolated, while prod keeps cross-replica Redis limits. No test asserts 429 (grep-confirmed), so behaviour under test is unchanged. **Verified by reproduction:** full suite with `REDIS_URL` set went **86 failed → 104 passed**; full CI-equivalent `turbo lint typecheck test` (REDIS_URL set) = **10/10 tasks green, api 104 · web 32**. | — |
| 2026-07-09 | P3.8 | **C1 — `ollama` compose service under the `ai` profile.** New `ollama` service in `docker-compose.yml` (`image: ollama/ollama:latest` — needs ≥0.5 for JSON-schema `format`; `profiles: [ai]`; named volume `ollama_models`; **no `ports:`** so it's reachable only on the compose network at `http://ollama:11434`; `OLLAMA_KEEP_ALIVE=30m`; 10g mem limit). Wired `OLLAMA_BASE_URL`/`OLLAMA_MODEL`/`AI_TIMEOUT_MS` env passthrough into the `api` service (empty base-url = AI off, no rebuild to toggle). `.env.example` AI block already added in A1. `docker compose --profile selfhost-data --profile ai config` validates. **Not started/pulled yet** — the 5 GB `qwen2.5:7b` pull + live run is the Part-B verification step (host-Ollama on Apple Silicon is an option per the memory note); done when wiring the buttons. Free/local — no owner checkpoint (that gates C2/C3 only). Local on `dev`. | — |
| 2026-07-09 | P3.8 | **A4 — `POST /api/ai/project-summary` — Part A complete.** Ephemeral status digest `{headline, bullets[], risks[]}` (not persisted — the frontend caches it in react-query only). Body `{projectId}`, `assertOrgRole(MEMBER)`, rate-limited **5/min** (heaviest context). Context reuses `projectOverview()` — status counts, active-sprint progress, top blocker **titles** (capped 120 chars), next milestones + readiness, recent velocity — deliberately **no full ticket descriptions** (small, PII-light payload). Tests (+2): happy digest · non-member 403. **api 102 → 104** (Part A exit; plan estimated ~94–97, landed higher via fuller coverage), full suite **104/104**, typecheck green. **Part A (api: Ollama client + 3 endpoints, all hermetic) DONE.** Next **C1** (free local `ollama` compose service) then **B1–B4** (wire buttons) → ⛔ owner checkpoint before C2/C3. Local on `dev`. | — |
| 2026-07-09 | P3.8 | **A3 — `POST /api/ai/expand-ticket`.** Expands an existing ticket into fuller `{description, acceptanceCriteria[], goal, constraints}` (draft only; the drawer fills editable fields, user saves via the normal PATCH). Body `{ticketId, prompt? ≤2000}`; loads the ticket + its `project.orgId`, `assertOrgRole(MEMBER)`; rate-limited **10/min**. Context = title + current description/AC/goal/constraints (each char-capped so the payload fits `num_ctx` 4096) + the user's optional steer. Tests (+2): happy path · unknown ticket → 404. **api 100 → 102**, typecheck green. Local on `dev`. | — |
| 2026-07-09 | P3.8 | **A2 — `POST /api/ai/draft-ticket`.** Drafts a work ticket from rough notes and **never creates it** (returns `{ draft: {title, description, acceptanceCriteria[], priority} }`; the client composes it into the existing create flow). Body `{projectId, notes 1..4000}`; resolves project → `assertOrgRole(MEMBER)`; rate-limited **10/min**. Senior-PM system prompt (concrete, no invented requirements, priority ∈ `URGENT|HIGH|MEDIUM|LOW`); output constrained by JSON-schema (`format`) + mirrored zod. Tests (+6): happy path (asserts no ticket persisted) · malformed-once → corrective retry → success · malformed-twice → **502 AI_BAD_OUTPUT** · disabled → **503 AI_UNAVAILABLE** · notes>4000 → 400 · non-member → 403. **api 94 → 100**, typecheck green. Local on `dev`. | — |
| 2026-07-09 | P3.8 | **A1 — `AIProvider` seam + Ollama impl + config + health.** Opens Phase 3.8 (local AI ticket drafting). New `services/ai.service.ts` built as the **permanent provider seam** (owner's three-tier strategy): `interface AIProvider { generate<T>(...); health() }`, `class OllamaProvider` (POST `/api/chat`, `stream:false`, `format:<json-schema>`, `temperature:0.2`, `num_ctx:4096`, `AbortSignal.timeout`; on parse/validate failure → **one corrective re-prompt** then throw), and `resolveProvider(orgId)` = the **single BYOK switch point** (returns Ollama iff `OLLAMA_BASE_URL` set, else `null` = AI off). Typed failures via `ApiError`: 503 `AI_UNAVAILABLE`, 504 `AI_TIMEOUT`, 502 `AI_BAD_OUTPUT`. `config.ts` gains `OLLAMA_BASE_URL` (default `''` = disabled, trailing-slash-trimmed), `OLLAMA_MODEL` (`qwen2.5:7b`), `AI_TIMEOUT_MS` (120000) + `.env.example` AI block. New `routes/ai.ts` `GET /api/ai/health` (`requireAuth`) → `{enabled, reachable, modelReady, provider}` drives the frontend's disabled-with-reason. Hermetic tests (`ai.test.ts`, `fetch` stubbed via `vi.stubGlobal`): disabled / enabled+modelReady / model-absent / unreachable / auth-required. **api 89 → 94**, typecheck green. Plan: `agentpm-plan/phases/phase-3.8-ai-tickets.md`. Local on `dev` (unpushed). | — |
| 2026-07-08 | P3.7.4 | **Post-phase verification pass + B2 TOCTOU fix.** Full re-verification of 3.7.4 mirroring CI exactly: `pnpm audit --prod --audit-level high` **exits 0** (1 low — confirms the D1 gate passes, not just prints), `turbo lint typecheck test` green (**api 89/89 · web 32/32**), all **12 spec'd audit actions** grep-confirmed wired, `/health` `/ready` `/documentation` 200, boot retention sweep logged, owner browser-verified light-mode contrast tokens (5.58:1 / 5.83:1 computed live), GDPR cards, and Board `aria-label`. **One defect found & fixed:** `DELETE /api/me`'s sole-owner guard ran *outside* the erasure transaction — a concurrent demotion of an org's other owner in that window could leave the org ownerless. The guard's invariant is now **re-verified inside the interactive transaction** after the caller's memberships are deleted (count==0 → throw `SOLE_OWNER` → full rollback). gdpr+audit tests 6/6, full suite green, api restarted clean. | — |
| 2026-07-08 | P3.7.4 | **axe-core a11y gate + fixes (E1) — PHASE 3.7.4 COMPLETE.** New `e2e/a11y.spec.ts` (`@axe-core/playwright`) audits **Dashboard, Board, List, Account** against WCAG 2.0/2.1 A+AA and **fails on any critical/serious** violation (moderate/minor logged only). The gate immediately caught **3 real defects, now fixed**: (1) **critical `select-name`** — the Board sort `<select>` had no accessible name → added `aria-label` (`board.sortLabel`); (2) **critical `label`** — the Account name/avatar/email inputs had unassociated `<label>`s → added `htmlFor`/`id`; (3) **serious color-contrast** across many spots — the avatar-fallback initials + all `text-muted-foreground`-on-`bg-muted` (role pills, key badges, filter toggles) sat at ~4.34:1, and light-mode `text-destructive` (danger-zone titles, BlockedBadge) at ~3.76:1, both below AA 4.5:1. Fixed at the token level (the light-mode mirror of the 3.7.2 P3 dark fix): `--muted-foreground` 46.9%→**40% L**, `--destructive` 60.2%→**45% L** (still clearly red), plus an explicit `text-foreground` on `AvatarFallback`. Re-audit: **0 serious/critical on all 4 pages** (0 moderate/minor too). web typecheck + **32/32** + vite build green. **Infra:** seeded the Playwright e2e Keycloak user (dev realm ships none) + committed an idempotent `infra/keycloak/seed-e2e-user.sh` for reproducibility; installed the Playwright chromium browser. *(Note: Sanjay's browser session had expired by the final check, so the contrast fixes were verified by axe — authoritative for contrast — rather than a screenshot; token changes are conservative darkenings and low-risk, but a light-mode eyeball is worth a glance next login. The a11y test creates throwaway orgs under the isolated `e2e-a` test account.)* **Phase 3.7.4 COMPLETE — all of A/B/C/D/E landed; api 89/89, web 32/32.** | — |
| 2026-07-08 | P3.7.4 | **Retention purge job (E2).** New `services/retention.service.ts` `purgeExpired(now?)` — deletes **read** notifications older than `RETENTION_NOTIFICATION_DAYS` (new config, default 90) and **unaccepted** invites expired >30 days, in one transaction, returning counts. Deliberately leaves archived tickets/projects (owner keeps forever), audit rows (compliance evidence), and **unread** notifications. Scheduled in `start()` — not `buildServer()`, so tests stay timer-free — once on boot then daily via `setInterval(...).unref()`. `RETENTION_NOTIFICATION_DAYS` added to `config.ts` + `.env.example`. New `src/test/retention.test.ts` seeds a stale-read notif + recent-read + old-**unread** + long-expired invite + freshly-expired invite, asserts exactly the two stale rows go and the other three stay → **api 88 → 89.** Full suite green; container restarted and boot logs show `retention sweep complete`. | — |
| 2026-07-08 | P3.7.4 | **Redis-backed + tiered rate limiting (D2) — Part D complete.** `@fastify/rate-limit` now uses an **ioredis** client when `REDIS_URL` is set (reuses the existing env var; short `connectTimeout:500`/`maxRetriesPerRequest:1` so a Redis blip degrades to the in-process store rather than stalling) — limits now hold across API replicas and survive restarts. Without `REDIS_URL` (tests, dev-without-redis) it stays on the in-memory store, so the suite remains hermetic. `rlRedis.quit()` added to the existing `onClose`. **Tiered caps** on abuse-prone writes via per-route `config.rateLimit`: org-create + invite-create → **20/min**, invite-accept → **30/min** (a leaked/guessed token can't be hammered). Import fix: ioredis needs the **named** `{ Redis }` export under ESM (default import isn't constructable). **api 88/88** (hermetic, no new tests — the plugin is upstream-tested). Container rebuilt; **live-verified** the Redis store is active: after an authenticated page load, `redis-cli KEYS '*'` showed a `fastify-rate-limit-*` key with a request counter (6) and a 51s TTL. Did **not** drive org-create to 429 (would create 20 junk orgs on the real dev account); threshold enforcement is the plugin's job. **Phase 3.7.4 Part D (supply chain + rate-limit maturity) done.** | — |
| 2026-07-08 | P3.7.4 | **CI SCA gate + Dependabot (D1) — found & fixed 5 real CVEs.** Added `pnpm audit --prod --audit-level high` to the CI test job (before prisma generate) + `.github/dependabot.yml` (weekly npm `/sourcecode` + github-actions `/`, 5-PR cap). **The gate immediately caught live vulns in the auth path:** 4 **critical** + 1 high in `fast-jwt` (pulled by `@fastify/jwt@9.1.0` → `fast-jwt@5.0.6`) — incl. an **async-key-resolver auth-bypass on empty HMAC secret** and a **cache-confusion claims-mixup**, both directly on our Keycloak token-verification path — and 1 **critical** in `get-jwks@9.0.2` (**poisoned JWKS cache → post-fetch issuer-validation bypass**, exactly our JWKS resolver). Fixed by upgrading `@fastify/jwt` 9→**10.1.0** (brings `fast-jwt@6.2.4`) and `get-jwks` 9→**11.0.3**. `pnpm audit` now: **1 low only** (passes the high gate). Typecheck clean (both are drop-in — no API changes needed); **api 88/88** unchanged. Container rebuilt; **live-verified** the whole auth path still works: `/ready` 200, and the browser My-Work page loaded with every `/api/me`, `/api/orgs`, `/api/projects` call returning 200 (no 401s) — real Keycloak tokens still verify under the new libs. | — |
| 2026-07-08 | P3.7.4 | **Caddy HSTS + SPA CSP (A2) — Part A complete.** All three prod sites (`infra/caddy/Caddyfile`) get `Strict-Transport-Security`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`. The app domain additionally gets a **`Content-Security-Policy-Report-Only`** (deliberately not enforcing yet — a wrong `connect-src`/`frame-src` would brick login, since keycloak-js uses an iframe for silent check-sso): `default-src 'self'`, `connect-src` allows the API (https+wss) and auth domain, `frame-src` allows the auth domain, `frame-ancestors 'none'`. The auth domain keeps no `frame-ancestors` restriction (the SPA iframes it) and Keycloak's own CSP is left alone. Validated with a throwaway `caddy:2` container (`caddy validate`) → **"Valid configuration."** No live Caddy in dev, so this is config-only until the next prod deploy — **follow-up required then:** watch DevTools console for CSP-report violations during login/board/WS, then promote `Content-Security-Policy-Report-Only` → `Content-Security-Policy`. **Phase 3.7.4 Part A (security headers) done.** | — |
| 2026-07-08 | P3.7.4 | **`@fastify/helmet` — OWASP baseline headers (A1).** Registered right after `cors`, before `rateLimit`, with `contentSecurityPolicy: false` and `hsts: false` (TLS terminates at Caddy — A2 sets HSTS there; a default CSP would break Swagger UI at `/documentation`, and the API serves no user-facing HTML anyway). New assertion in `health.test.ts` for `x-content-type-options: nosniff` + `x-frame-options`. **api 87 → 88.** Container rebuilt (new dependency) + `docker compose up -d api`; live-verified via curl: `X-Content-Type-Options`, `X-Frame-Options: SAMEORIGIN`, `Referrer-Policy: no-referrer`, `X-DNS-Prefetch-Control: off`, `X-Download-Options`, `X-Permitted-Cross-Domain-Policies`, `X-XSS-Protection: 0` all present; `/documentation` still 200s and renders. **Aside, unrelated to this change:** the fresh container's boot logs showed 3 requests to `/documentation` from a non-localhost IP within seconds of restart — flagged to the owner as a possible externally-reachable dev port worth checking (the api service binds `0.0.0.0:3001` in `docker-compose.override.yml`), not investigated further as out of scope. | — |
| 2026-07-08 | P3.7.4 | **Web: export + delete-account in Account Settings (B3) — Part B GDPR rights complete.** New "Your data & privacy" card (Account, below Appearance): **Download my data** streams the export as a blob (new `api.exportMyData`/`requestBlob` in `lib/api.ts` — extracted a shared `authedFetch` so JSON and blob requests reuse the same token-refresh path; filename comes from the response's `Content-Disposition`) and triggers a real browser download via an object-URL anchor click. **Delete my account** reuses the existing `DangerZone` component (type-your-email-to-confirm) — on success calls `logout()`; on `409 SOLE_OWNER` shows the server's org-list message inline (`FieldError`-style, not a toast) instead of signing out. `ApiError` gained an optional `code` field so the UI can branch on it. i18n under `account.privacyTitle`/`exportData`/`exportDownloaded`/`deleteTitle`/`deleteWarning`/`deleteAction`; FEATURES updated + re-dated. **Browser-verified** on the real dev account (sanjaykumarp041@gmail.com) at `/account`: both cards render correctly; clicked **Download my data** for real — toast "Export downloaded", no console errors, api logs show `GET /api/me/export` → 200, and a fresh `account.exported` audit row confirmed via `psql`. Did **not** exercise the delete-account button against the real account (irreversible on a live account) — that path is covered by the B2 API tests instead. web typecheck + vite build + 32/32 green; api untouched (87/87). | — |
| 2026-07-08 | P3.7.4 | **GDPR erasure by anonymization (B2).** New `DELETE /api/me` (GDPR Art. 17). Blocks with `409 SOLE_OWNER` if erasure would leave any org ownerless (checked across every org the caller owns). Otherwise: unassigns their open tickets, deletes `orgMember`/`ticketWatcher`/`notification`/`commentReaction` rows, then **anonymizes** the user row (`email → deleted-<id>@anonymized.invalid`, `name → 'Deleted user'`, `avatarUrl`/`idpSub` cleared) rather than hard-deleting — `Ticket.createdBy` is `onDelete: Restrict`, so a real delete would 500 for anyone who ever created a ticket. Their created tickets/comments remain, now attributed to "Deleted user"; the Keycloak account itself is untouched (API holds no IdP admin creds) — re-login JIT-provisions a fresh account. Records `account.erased`. 2 new tests in `gdpr.test.ts` (sole-owner block; full anonymize + ticket/audit-row assertions) — **api 85 → 87**, full suite green, typecheck clean, container restarted clean. | — |
| 2026-07-08 | P3.7.4 | **GDPR data export (B1).** New `GET /api/me/export` (GDPR Art. 20 portability) — one-shot JSON bundle: profile, org memberships, created + assigned tickets (`{key,title,status,createdAt}`), authored comments, watched ticket ids, notifications. `Content-Disposition: attachment` so it downloads as `agentpm-export-<date>.json`; records an `account.exported` audit row. New `src/test/gdpr.test.ts` — asserts the bundle shape and, critically, that a second org member's email never appears in the caller's export. **api 84 → 85**, typecheck clean, container restarted clean. | — |
| 2026-07-08 | P3.7.4 | **Audit service + org audit endpoint (C2+C3).** New `services/audit.service.ts` — fire-and-forget `audit()` that writes an `AuditLog` row and swallows failures (never fails the caller's request). Wired at 8 call sites: `org.created`/`org.deleted`, `member.role_changed` (with `{from,to}` meta)/`member.removed`, `invite.created`/`invite.revoked` (`routes/organizations.ts`); `project.archived`/`project.restored`/`project.deleted` (`routes/projects.ts`); `ticket.permanently_deleted` (`routes/tickets.ts`). New `GET /api/orgs/:slug/audit` (ADMIN-only, cursor-paginated via the existing `lib/pagination.ts`, optional `?action=` prefix filter, batch-resolves actor display names with no FK join since `AuditLog` intentionally has none). New `src/test/audit.test.ts` (role-change meta, permanent-delete row, ADMIN-vs-MEMBER on the list endpoint) — **api 81 → 84**, typecheck clean, container restarted clean. | — |
| 2026-07-08 | P3.7.4 | **`AuditLog` schema + migration (C1).** New append-only `AuditLog` model (`orgId?`, `actorId?`, `action`, `targetType`, `targetId?`, `meta? Json`, `createdAt`) — deliberately **no FK relations** so rows survive org deletion and user erasure (unlike `TicketActivity`, which cascades). Migration `20260708025328_audit_log` (additive `CREATE TABLE` + `@@index([orgId, createdAt])`), applied inside the api container, client regenerated host + container, api restarted clean. Opens Phase 3.7.4 (compliance & security hardening, spec: `agentpm-plan/phases/phase-3.7.4-compliance-hardening.md`) — closes the P0/P1 gaps from the standards audit (`release-doc/ARCHITECTURE-AND-COMPLIANCE-REPORT.md`). No app code wired yet (C2 next). | — |
| 2026-07-07 | P3.7.3 | **Archived-projects page + DangerZone→Archive (B3) — PHASE 3.7.3 COMPLETE.** New per-org route `/orgs/:slug/archived` (`ArchivedProjects.tsx`) listing archived projects with **Restore** (`restoreProject`) + **Delete permanently** (`deleteProject`, inline two-click confirm + 4s reset, `Loader2`, shared `EmptyState`); "Archived" link in the OrgProjects header. Project **Danger Zone now archives** (recoverable) instead of hard-deleting (`archiveProject`; copy points to Archived projects; keeps typed-name confirm). api client: `archiveProject`/`restoreProject` + `listProjects(orgId,{archivedOnly})`. i18n `archived.projects*` + `settings.archive*`. **Browser-verified** with a throwaway project (ZAT): archive hid it from grid+sidebar → `/archived` listed it → Restore returned it (EmptyState after) → re-archive → permanent delete removed it entirely; workspace restored. web 32/32 + typecheck + vite build green. FEATURES updated. **Phase done: api 81/81 · web 32/32.** | — |
| 2026-07-07 | P3.7.3 | **Project soft-archive: schema + API (B1+B2).** `Project.archivedAt` + `@@index([orgId, archivedAt])` (migration `20260707034922_project_archive`; generated host + container + restart). New `POST /api/projects/:id/archive` + `/restore` (ADMIN); `DELETE /:id` stays permanent. List hides archived by default + `?archivedOnly=true`. Archived projects excluded from org project count (`organizations.ts`), sidebar stats (`stats.service.ts`), project + ticket **search** (`search.ts`), and **my-work** (`me.ts` `memberOf.project.archivedAt: null`) — so an archived project's tickets stop surfacing everywhere. Tests +2 → **api 81/81**, full suite green (no regressions from the shared-query guards); typecheck clean. | — |
| 2026-07-07 | P3.7.3 | **Archived-tickets page + restore + permanent delete (A2).** New per-project route `/…/archived` (`ArchivedTickets.tsx`) listing archived tickets (`listTickets` with `archivedOnly=true`, `-updatedAt`) with **Restore** (batch `{archived:false}`) and **Delete permanently** (inline two-click confirm + 4s reset, `Loader2`, shared `EmptyState`); "Archived" link in the List toolbar; `archived.*` i18n. **Scope grew:** `DELETE /api/tickets/:id` is itself the *soft*-delete, so a real **`DELETE /api/tickets/:ticketId/permanent`** (ADMIN → `prisma.ticket.delete`; relations cascade, subtasks SetNull) + web `api.deleteTicketPermanent` + test were added (**api 78→79**). **Browser-verified** (dark): found RELA-5 wrongly archived by the P5 test automation → **restored** to the board; **permanently deleted** the RELA-6 throwaway (closing that 3.7.2 loop); RELA-4 (owner's real archived ticket) untouched. web 32/32 + typecheck + build green; api 79/79. FEATURES updated. | — |
| 2026-07-07 | P3.7.3 | **`archivedOnly` ticket list filter + phase open (A1).** New Phase 3.7.3 (Archive & Restore) — owner asked for a place to see/retrieve archived tickets or projects; RELA-6 (3.7.2 P5) had nowhere to go. Backend was already close: tickets soft-delete via `archivedAt`, list has `includeArchived`, batch already **unarchives** (`{archived:false}`). A1 adds `archivedOnly` to `routes/tickets.ts` `listQuerySchema` → `where.archivedAt = { not: null }` (for the Archived view). Test in a **new `src/test/archive.test.ts`** (not `tickets.test.ts` — a new `it` there tripped a latent within-file ordering fragility; the route change alone is clean, verified by stashing). **api 77→78**, typecheck green. Phase doc + README indexed. | — |
| 2026-07-07 | P3.7.2 | **Global `:focus-visible` polish (P6) — PHASE 3.7.2 COMPLETE.** Added a global `:focus-visible { outline: 2px solid hsl(var(--ring)); outline-offset: 2px }` in `index.css` `@layer base` (after `body`). Reaches only the otherwise-naked interactive elements (card hover controls, reaction chips, slash/mention suggestion items, theme buttons); Button/Input/Textarea/TicketCard already set `focus-visible:outline-none` + their own ring so they opt out. **Browser-verified** via real keyboard Tab: the raw theme `<button>` gets `outline: 2px solid rgb(203,213,225)` (`--ring`) at 2px offset with `:focus-visible` matching; the shadcn Input opts out (`outline` transparent) and keeps its own `box-shadow` ring — no double-up. vite build + typecheck green; both variants present in the built CSS. **All P1–P6 landed** (6 commits); web 32/32, api untouched (77/77). | — |
| 2026-07-07 | P3.7.2 | **Bulk-archive confirm hardening (P5).** `BulkBar.tsx`'s two-click archive confirm (the F2-blessed inline pattern — kept, no modal) gained: a **count in the label** (`bulk.confirmArchiveN` = "Archive {{count}}?", replacing the retired `bulk.confirmArchive` = "Confirm?"), a **4s auto-reset** `useEffect` (alongside the existing `onBlur` reset), and a **`Loader2` spinner** swapped in for the `Archive` icon while `busy && confirmArchive`. **Browser-verified** (dark, definitive DOM/high-freq probes): armed label reads "Archive 3?" destructive; disarms after 4.3s untouched; disarms on blur; a throwaway ticket (RELA-6) selected → confirm → spinner svg present through the in-flight window, 1 batch request, ticket archived. Cleanup note: RELA-6 remains **archived** (owner chose to leave it — this UI has no archived view/drawer to delete it, and the API path needs the auth token, correctly blocked). web typecheck + vite build + 32/32 green; api untouched. | — |
| 2026-07-07 | P3.7.2 | **Shared `EmptyState` component (P2).** New `components/EmptyState.tsx` (muted lucide icon + message + optional `cta` slot, on Dashboard's bordered-card look; `className` enables a `border-0 bg-transparent py-6` bare variant). Migrated all **6** divergent renderings to it — Dashboard (`Building2`), OrgProjects (`FolderKanban`), Sprints (was a bare `<p>` → `Rocket`), ProjectGantt (`CalendarRange`) as cards; ProjectList (bare, inside the `<td colSpan>` — `Inbox`/`Filter` by `hasFilters`) and MyWork (bare, inside the `<li>`; `section` helper gained an icon arg — `Inbox`/`Eye`). All reuse existing i18n keys (no new copy). New colocated `EmptyState.test.tsx` (renderToStaticMarkup — icon+message, cta slot, className) → **web 31 → 32**. **Browser-verified** (dark): List impossible-filter shows the bare Filter-icon "No tickets match these filters." row; Knowledge Graph Sprints shows the Rocket card "No sprints yet." typecheck + vite build green; api untouched. | — |
| 2026-07-07 | P3.7.2 | **Spinner-in-button busy feedback (P4).** First `Loader2` in the codebase, on the 4 worst async buttons: `DangerZone` (replaced `{busy ? 'Loading…' : label}` with spinner + label; dropped its now-unused `useTranslation`/`t`), and the 3 settings Saves that had **no busy state at all** — `AccountSettings`, `ProjectSettings`, `OrgSettings` (added `busy` set in a try/finally around the await; `{busy && <Loader2 className="h-4 w-4 animate-spin" />}` before the label + `disabled={… || busy}`). Fixes the double-submit bug on all three. **Browser-verified** on Account via an in-page high-frequency DOM probe (single click → button disabled with the spinner svg present through the whole in-flight window `D1 D1 … D0`, exactly **1** PATCH fired per click); avatar test-value restored to the original after. Note: the real save is fast, so a spinner is a brief flash on a good network — the probe (not screenshots) is what caught it deterministically. web typecheck + vite build + 31/31 green; api untouched. | — |
| 2026-07-07 | P3.7.2 | **Blocked badge → `--destructive` token + shared `BlockedBadge` (P3).** Dark `--destructive` brightened `0 62.8% 30.6%` → `0 72% 51%` (the old value was too dark to read as *text*, which is why every badge hand-rolled `dark:text-red-300`). New `components/BlockedBadge.tsx` (`bg-destructive/10 text-destructive` pill; `count?`/`showIcon?`/`title?` + an added `className?` for the drawer's `ml-auto`) replaces **5** hand-rolled hardcoded-red sites: TicketCard, ProjectList, MyWork, TicketDrawer collapsed header (`showIcon={false}`), ProjectOverview blockers (the divergent `bg-red-500/10 text-red-500`, `count` variant). Dropped the now-unused `Ban` import from the 4 pages. `grep bg-red-100` clean (only a comment left). **Browser-verified in dark mode**: temp RELA-1←RELA-2 blocker → readable badge on the board card (label) + Overview blockers card (count); Danger-zone heading + Delete button readable/not muddy after the token change; blocker removed after. **Found & flagged separately:** the drawer collapsed-header badge is dormant (`ticket.blockedBy` isn't returned by `api.getTicket`) — pre-existing, guard preserved verbatim. web typecheck + vite build green; web 31/31, api untouched. | — |
| 2026-07-07 | P3.7.2 | **Inline field validation pattern (P1).** New reusable idiom: `aria-invalid` red border on `ui/input.tsx` + `ui/textarea.tsx` (via the arbitrary-variant `aria-[invalid=true]:border-destructive …:ring-destructive` — `aria-invalid` is *not* a default Tailwind 3.4 aria variant, so the bare `aria-invalid:` form the plan named compiles to nothing; verified the `aria-[invalid=true]` selector is present in the built CSS) + new `ui/field-error.tsx` (`role="alert"`, `text-destructive`). Applied to the two audited offenders: `AccountSettings` name (red border + "Name is required." when blank, Save stays disabled) and `TicketDrawer` Start/Due (compares `slice(0,10)` before `patch`; start>due → inline red + "Start must be on or before the due date.", **no network call/toast**, server `DATE_RANGE` 400 kept as backstop). i18n `account.nameRequired`, `drawer.dateRangeError`. **Browser-verified** (dark mode): account name blank→red→clears on retype; RELA-1 start 2026-06-15 vs due 2026-03-09 → inline error, no save; cleared → error clears + saves; ticket restored (start null, due unchanged). web typecheck + vite build green; **web 31/31, api untouched (77/77)**; FEATURES ticket section + date updated. | — |
| 2026-07-06 | P3.7.2 | **UX audit + Phase 3.7.2 opened (P0).** Full UI/UX audit of the web app: foundation sound (shadcn-style tokens + dark mode + per-org accents, ⌘K/keyboard story, optimistic updates + undo, skeletons, Radix a11y); two suspected gaps disproved in code (slash-command popover exists since 3.2 C3; column widths persist via `agentpm-list-colwidths`). Six real polish gaps → new `phase-3.7.2-ui-ux-polish.md` (P1 inline validation — no `aria-invalid`/error-text anywhere · P2 empty states use 4 idioms, Sprints a bare `<p>` · P3 blocked badge hardcodes reds in 5 places + dark `--destructive` too dark for text · P4 zero `Loader2` usage, settings Saves double-fire · P5 bulk-archive confirm lacks count/reset/busy · P6 no global `:focus-visible` for raw buttons); README indexed. Owner constraint recorded: keep PMAgent's own identity — no Jira/Monday cloning. | — |
| 2026-07-06 | P3.7.1 | **Gantt rail narrows on mobile (F6, 3.7 R7.7) — PHASE 3.7.1 code COMPLETE.** `ProjectGantt` passes `narrow={!interactive}` (reuses the existing 640px `matchMedia` flag) into `GanttChart`; `RAIL_W` (240) → `railW = narrow ? 88 : 240` in the grid template, and the rail row drops the title span (keeps the mono key + drawer click) below `sm`. Frees ~64% of a phone's width for the bars. web typecheck + build + 31/31 green. **Browser-verified at 390px** (rail = key only, no title; desktop unchanged). **All F1–F6 landed** (6 commits); api 77/77 · web 31/31. | — |
| 2026-07-06 | P3.7.1 | **Due column in the List chooser (F5, 3.7 R13 "Due(?)").** Added `'due'` to `ProjectList.tsx`'s column-driven model (ColId/COL_ORDER/TOGGLEABLE/DEFAULT_VISIBLE default-off/DEFAULT_WIDTHS/COL_LABEL + a `renderCell` case mirroring `start`, reading `tk.dueDate`). i18n `list.colDue`. Everything else (chooser dropdown, `<colgroup>`, colSpan, resize handles) derives from the model; CSV export already emitted Due. web typecheck + build green. **Browser-verified** (enabling Due rendered the column with RELA-1's 2026-03-09; toggled back off after). | — |
| 2026-07-06 | P3.7.1 | **Subtask progress chips on board cards (F4, 3.7 R9.4).** New `subtaskCounts(parentIds)` in `relations.service.ts` (one `groupBy [parentId, status]` over non-archived, non-CANCELLED children → `{done,total}`; parents with no such children omitted). Wired into the tickets list handler alongside `blockedByCounts` (`Promise.all`), spread as `subtasks` per item. Web `Ticket` type gains `subtasks?: {done,total}`; `TicketCard` renders a muted `ListTodo` "done/total" chip when present (mirrors the blocked chip). i18n `board.subtasksTooltip`. API test +1 (parent {done:1,total:2}, CANCELLED excluded, childless absent) → **api 77/77**; typecheck + build green. **Browser-verified** (RELA-5 card showed a "0/2" chip after two subtasks were added inline; subtasks deleted + counter reset). FEATURES board section updated. | — |
| 2026-07-06 | P3.7.1 | **Expanded-sprint live sync (F3).** One-line fix in `Sprints.tsx`: added the bare `['sprint']` prefix to `useProjectSync` so foreign WS events invalidate every expanded sprint's detail (`['sprint', id]`, rendered by the R12 rows) — previously only local mutations did, so a teammate's change left expanded rows stale until you touched something. **Verified** via Redis foreign-actor injection: publishing `sprint.updated` to `agentpm:events` refetched `GET /api/sprints/:id` (only the new prefix triggers that endpoint). web typecheck green. | — |
| 2026-07-06 | P3.7.1 | **Milestone rename/re-date/delete UI (F2).** `ProjectOverview.tsx` Milestones card gains a **manage mode** (pencil toggle): lists ALL milestones (`['milestones', projectId]` query, enabled only in manage mode) with editable name + date `Input`s (blur → `api.updateMilestone`), the done-toggle, and a **delete** control gated by `isAdmin` (OWNER/ADMIN, mirroring `ProjectSettings`). Delete uses an **inline two-click confirm** (trash → red "Delete?" → deletes) rather than `window.confirm` — better UX and it doesn't wedge the tab. Every milestone mutation now invalidates `['overview']` + `['milestones']` + `['gantt']`, so the Timeline lane follows renames/deletes. `api.deleteMilestone` (previously call-less) now wired. i18n `overview.manageMilestones/deleteMilestone/milestoneDeleteConfirm/milestoneSaved/milestoneDeleted`. **Browser-verified** (rename + re-date reflected on the Gantt lane; delete removed it; seeded milestone cleaned up). web 31/31, typecheck + build green; FEATURES Overview section updated. | — |
| 2026-07-06 | P3.7.1 | **Start date in the ticket drawer (F1).** Added a **Start date** `Input type="date"` to the `TicketDrawer.tsx` Details grid, immediately before Due date, mirroring the Due block verbatim (`patch({ startDate: v })`; `Ticket`/`UpdateTicketInput` already carry `startDate` from 3.7 R1/R8). i18n `drawer.startDate`. **Browser-verified** on RELA-1: set 2026-03-01 → List Start column showed it; cleared → back to `—`; data restored (server `DATE_RANGE` guard for start>due already unit-tested). web typecheck green; FEATURES ticket-panel line updated. | — |
| 2026-07-06 | P3.7.1 | **3.7 audit + Phase 3.7.1 opened (F0).** Full audit of the "done" claim: every R0–R14 artifact verified in code (schema/rules/routes/hook/services/pages/components/i18n/FEATURES), **api 76/76 · web 31/31 · both typechecks · vite build green**, and the pending **multi-select filter browser check completed** (Board: MEDIUM → "MEDIUM +1" with LOW, cards narrowed/restored, Clear works; List: LOW → only RELA-1, Clear filters works; no data writes). Verdict: 3.7 core sound; **6 fine-print gaps** → new `phase-3.7.1-gap-closure.md` (F1 drawer Start date · F2 milestone rename/delete UI (`deleteMilestone` has no caller) · F3 expanded-sprint live sync (`['sprint', id]` not invalidated on foreign events) · F4 subtask chips (R9.4 cut) · F5 List Due column (R13 "Due(?)") · F6 Gantt mobile rail (R7.7)); README indexed. | — |
| 2026-07-05 | P3.7+ | **Board + List filters → multi-select.** New reusable `components/MultiSelect.tsx` (checkbox-list dropdown; trigger summarises "Label +N"). **Board** (priority/type/assignee/sprint) and **List** (status/priority/type/assignee/sprint/label/workstream) filters now hold **arrays** and send comma-separated params. API list filters accept CSV via a `csvOf` zod transform → Prisma `{ in: [...] }` for status/priority/type/assignedToId/labelId/sprintId/workstream (a single value is a 1-element list). Test: `?priority=URGENT,HIGH` returns both; single value still works. api 76/76, web 31/31, typecheck + build green. ~~Browser check pending~~ → **browser-verified 2026-07-06** during the 3.7.1 audit (see row above). | — |
| 2026-07-05 | P3.7 | **Release readiness on Reports (3.7 R14) — PHASE 3.7 COMPLETE.** `reports.service.ts` `projectReports` payload gains `readiness: ReadinessMilestone[]` (open milestones + due-date `done/total`, reusing R4's `milestoneReadiness`) and `overall: { done, open }` (one status `groupBy`, non-archived, non-cancelled). New **Release readiness** card in `ProjectReports.tsx`: hand-rolled completion **donut** (two `stroke-dasharray` arcs, center %, "done · open" caption) + per-milestone rows (name, date, slim progress bar, "{{count}} still open before this date"); empty state links to Overview. Web `ReadinessMilestone`/`ProjectReports` types + `reports.readiness*` i18n; FEATURES reports section (velocity sprint-only + workload split notes). API reports test extended (`overall` {done:2,open:1}, readiness window). **Browser-verified** (donut 25% + 4 milestone rows w/ open-caveat; seeded milestone removed). api 75/75, web 31/31, typecheck + build green. Phase doc → ✅ DONE. | — |
| 2026-07-05 | P3.7 | **List column chooser + resizable widths (3.7 R13).** Refactored `ProjectList.tsx` table to a column-driven model (`COL_ORDER`/`renderHeader`/`renderCell`; Select/Key/Title always shown). A **Columns** (`Columns3`) dropdown of `DropdownMenuCheckboxItem`s toggles Status/Priority/Assignee/Sprint/Workstream/Start/Points/Updated, persisted `agentpm-list-columns`. Added the new **Start** column (`startDate`, default off). Table is now `table-fixed` with a `<colgroup>`; each header has a **drag-to-resize** handle → per-column widths persisted `agentpm-list-colwidths` (owner-requested extra beyond the plan). th+td gated by one predicate; empty/skeleton colSpan derived from shown columns; **CSV export still exports all columns** (comment). i18n `list.colStart`/`list.columns`. **Browser-verified** (toggle+resize persist across reload; sort preserved; prefs reset after). web 31/31, typecheck + build green. | — |
| 2026-07-05 | P3.7 | **Sprint view: informative rows + inline actions + goal edit (3.7 R12).** `Sprints.tsx` expanded ticket rows enriched: key+title (click → `/board/ticket/:n` drawer), `PRIORITY_CLASS` pill, inline **status** `<select>` (`updateTicketStatus`), **assignee** avatar + quick-assign dropdown (`['members', slug]` query, Unassigned option, `updateTicket`), points chip, plus the kept move-to-sprint select — all through the existing `act()` refresh (invalidates `['sprint', id]` + `['tickets', pid]`; live via R3). **Editable sprint goal** under the name (italic "No goal set" when empty; pencil → inline Input, Enter saves via new `api.updateSprint`). Per-sprint **assignee/status mini-filter** header (client-side over the expanded list). New i18n `sprints.noGoal/goalPlaceholder/goalSaved/statusChanged/reassigned/pts`; FEATURES sprint section. **Browser-verified** (rows render with status/assignee/priority/move controls; goal typed→persisted→cleared). web 31/31, typecheck + build green. | — |
| 2026-07-05 | P3.7 | **Workstream UX: tabs, list, CSV, bulk (3.7 R11).** Sprint-vs-ad-hoc surfaced everywhere (server rules from R1): **Board** header **All / Sprint work / Ad-hoc** segmented tabs (persisted `agentpm-board-ws-<pid>`, passes `workstream` to the tickets query; quick-add on the Ad-hoc tab creates ADHOC + no sprint). **Drawer** Workstream select + inline "switching to ad-hoc removes it from its sprint" hint; `changeWorkstream` invalidates sprints. **List** workstream filter + a `md:`-only Workstream column (colSpan/skeleton bumped 9→10). **BulkBar** "Set workstream" menu (`BatchPatch.workstream`). **CSV** (`CsvTools`): export gains `Start`+`Workstream` cols; `HEADER_ALIASES` + `WORKSTREAM_MAP` (sprint→SPRINT, ad-hoc/adhoc/ops→ADHOC, unknown omitted), start date → UTC ISO; `SAMPLE_CSV_ROWS` + `ImportTicketRow` updated. **Reports**: `workloadReport` groupBy gains `workstream` → per-person `sprintCount`/`adhocCount` split (WorkloadRow + ProjectReports row). Tests: `CsvTools.test.tsx` +1 (start/workstream mapping); api +2 (ADHOC import lands sprint-less; autoTodoOnAssign keeps ADHOC+sprint-less). **Browser-verified** (tabs filter, drawer select+hint). web 31/31, api 75/75, typecheck + build green. | — |
| 2026-07-05 | P3.7 | **Beta AI affordances (3.7 R10).** New `components/BetaBadge.tsx` — `BetaBadge` (single source for the "Beta"/"Arrives with the agent release" copy) + inert `BetaAIButton` (Sparkles + label + badge; `disabled`, `aria-disabled`, `title` tooltip, **no handlers, no network**). Placed at three spots (all disabled): board column quick-add ("Draft with AI"), `TicketDrawer` spec header ("Auto-fill from prompt"), and the R5 Overview summary card (swapped its inline Badge → `BetaBadge`). i18n `common.beta*` (existing) + `beta.draftWithAI`/`autoFill`. Grep-checked: no `anthropic`/`openai`/AI fetch in `apps/web/src`. **Browser-confirmed** "Draft with AI" renders in the quick-add. web 30/30, api 73/73, typecheck + build green. | — |
| 2026-07-05 | P3.7 | **Quick-add tokens + inline subtask create (3.7 R9).** Board **Column** quick-add now pipes the raw input through `parseQuickCreate(members, sprints)` (`!priority @assignee #sprint`) and renders live **preview chips** (priority pill, assignee avatar+name, sprint) below the input; `onQuickAdd` passes the parsed fields; a per-column **FilePlus2 template** dropdown (`['templates', orgId]`) creates from a template + opens the drawer. **Inline subtask create** in `RelationsSection`: an Enter-to-create input under the Subtasks list → `api.createTicket({parentId, workstream: parent.workstream})`, refresh + toast + clear + keep-focus for fast batch entry (existing "Link existing" picker kept). **TicketCard** gains a hover/focus-visible **+ subtask** (`ListPlus`) button → opens the drawer with the input focused via `focusSubtask` location state. Web `CreateTicketInput` gains `parentId`/`workstream`/`startDate`/`dueDate`. New `parseQuickCreate.test.ts` (3). i18n `board.quickAddHint/addTicket` + `relations.newSubtask*`/`addSubtask`/`linkExisting`/`subtaskCreated`; FEATURES creation + Tickets sections. **Browser-verified** (tokens→RELA-6 w/ all fields; 3 inline subtasks; test data deleted + counter reset). web 30/30, api 73/73, typecheck + build green. | — |
| 2026-07-05 | P3.7 | **Gantt interactivity: drag move/resize/schedule + undo (3.7 R8).** Pointer-driven drag on the SVG (no dnd-kit): bar body = move, 8px edges = resize-start/end (shown when bar ≥24px), live preview via pure `applyDrag`; **Escape** cancels; window pointer/keydown listeners; disabled below `sm` (matchMedia). Drop → `api.updateTicket({startDate,dueDate})` with optimistic `qc.setQueryData(['gantt',pid])`, rollback on error, and a **sonner Undo toast** (counter-PATCH to captured prev dates). **Tray drop**: HTML5 draggable chips → `traySchedule(day)` = 3-day bar. **Milestone diamonds draggable** → PATCH `date` + undo. Guard: `useProjectSync` gains an optional `pausedRef` — suspended (`dragPaused`) during an active drag so a teammate's WS echo can't yank the bar. Added `traySchedule`/`DragKind` to `lib/gantt`; web `UpdateTicketInput` gains `startDate`/`workstream`; `gantt.rescheduled`/`milestoneMoved` i18n; FEATURES drag line. Tests: `gantt.test.ts` +1 (traySchedule 3-day). **Browser-verified** (move+undo, tray→3-day bar, milestone drag; data restored). web 27/27, api 73/73, typecheck + build green. | — |
| 2026-07-05 | P3.7 | **Read-only Gantt page (3.7 R7).** New `pages/ProjectGantt.tsx` (route `…/gantt`) + pure presentational `components/gantt/GanttChart.tsx` (one hand-rolled `<svg>`, BurndownSparkline precedent): sticky left rail (mono key + title, click→`/board/ticket/:n`) aligned row-for-row with a scrollable timeline — status-colored bars (`STATUS_COLOR` added to `lib/board.ts`; 60% fill + 3px left edge; label when >120px), header tick labels + gridlines (majors bolder), weekend shading (day scale), today line, milestone lane (amber diamond + dashed line; done muted), dependency elbow paths with an arrowhead `marker`. Controls: assignee/sprint/label/status/workstream selects (client-side, persisted) + D/W/M toggle (`agentpm-gantt-scale`) + Today scroll; collapsible **Unscheduled** tray; `truncated` banner; `useProjectSync([['gantt',pid]])`. Added `labelIds` to the R6 gantt payload (service + web type) for the label filter. Nav: OrgTree **Timeline** leaf (`GanttChartSquare`) between Sprints & Reports + ⌘K entry; `gantt.*` + `nav.timeline` i18n; FEATURES.md Timeline section. **Browser-verified** (Day/Week scales, milestone, dependency arrow, filter narrowing, drawer from rail; seed data restored). web 26/26, api 73/73, typecheck + build green. | — |
| 2026-07-05 | P3.7 | **Gantt data + pure date math (3.7 R6).** New pure module `apps/web/src/lib/gantt.ts` — no React, no local-time: everything runs on **UTC day numbers** so DST/timezone can't shift a bar or tick. `toDayNum`/`dayNumToISO` (round-trip), `xForDay`/`dayForX` (inverse per scale), `barForTicket` (4 date combos; due-/start-only ⇒ 1-day), `clampBar`, `computeRange` (min/max ± 7d, always contains today; empty ⇒ [today-7,today+21]), `ticks` (day/week/month, Mondays major, "4 Jul" / "Jul 2026"), `applyDrag` (move/resize with edge-aware clamp). API: `services/gantt.service.ts` `projectGantt()` behind `GET /api/projects/:id/gantt` (MEMBER) → `{ items(dated tickets, key), edges(deps both on-screen), milestones, truncated(>1000, take 1000 updatedAt desc) }`; web `GanttPayload`/`GanttItem`/`GanttEdge` + `getProjectGantt`. Tests: `lib/gantt.test.ts` (8 — round-trip, inverse, bars, range, day/week/month ticks incl. a **DST-boundary** non-event, drag clamp) + api `gantt.test.ts` (2 — shape/edges/truncated + outsider 403). **web 26/26, api 73/73**; typechecks green. No UI yet (R7). | — |
| 2026-07-05 | P3.7 | **Overview page + landing switch (3.7 R5).** New `pages/ProjectOverview.tsx` — a manager's dashboard on a responsive 2-col card grid: **Status** (`MetricChip` open/done + reused `StatusBar` + workstream split), **Active sprint** (progress bar, days-left, `BurndownSparkline`, empty→Sprints), **Blockers** (top-5, mono key + red `Ban` count, click→`/board/ticket/n`), **Milestones** (next-3 open, inline readiness donut, done-toggle PATCH + inline add-form), **Capacity** (workload rows idiom + recent-velocity line), **Activity** (`ActivityFeed`), **AI summary** (Sparkles + Beta badge + disabled button, no network). `useProjectSync([['overview',pid],['tickets',pid]])`; skeletons while loading. **Landing switch (`App.tsx`):** project root → Overview, Board → `/board` + `/board/ticket/:n`, **legacy `/ticket/:n` kept** for old deep links. Updated every URL producer: `OrgTree` (Overview+Board+List leaves), `ViewToggle`, `ProjectMenu`, `CommandPalette` ticket hits, `NotificationBell`, `Board` drawer/quick-create nav. i18n `overview.*` + `nav.overview/list` + `common.beta*`; FEATURES.md Overview section. **Browser-verified** (see phase doc). web typecheck + 18 tests + vite build green. | — |
| 2026-07-05 | P3.7 | **Overview aggregate endpoint (3.7 R4).** New `services/overview.service.ts` → `projectOverview(projectId)` behind `GET /api/projects/:projectId/overview` (MEMBER). Single round trip, all pieces in one `Promise.all`, each reusing an existing helper: **status** = one `groupBy [status, workstream]` (byStatus + open/done + SPRINT/ADHOC split), **activeSprint** = `projectListStats` idiom (endDate→ISO), **blockers** = top-5 of `BLOCKED status ∪ dependencies.some`, ranked by open-dep count from `blockedByCounts`, **milestones** = next 3 open + due-date `readiness`, **capacity** = `workloadReport` rows + `recentVelocityAvg` (mean of last 3 non-null `velocityReport` points). Added reusable `milestoneReadiness(projectId)` to `reports.service.ts` (bucket non-archived, non-CANCELLED tickets by `dueDate` into open-milestone windows; R14 reuses it). Web `ProjectOverview`/`OverviewBlocker`/`OverviewMilestone` types + `getProjectOverview` in `api.ts`. New `overview.test.ts` (2): full-shape assert on a seeded fixture (mixed statuses/workstreams, one blocked pair, one milestone) + outsider 403 → suite **71/71**; typechecks green; api restarted. | — |
| 2026-07-05 | P3.7 | **`useProjectSync` live sync (3.7 R3 — code).** New `useProjectSync(projectId, prefixes)` in `lib/websocket.ts`: wraps `useProjectWebSocket`, invalidates each query-key prefix on `ticket.created/updated/deleted`, `sprint.updated`, `milestone.updated`, and on reconnect (`onReconnect`); reads `['me']` for the socket's existing self-echo drop. Adopted in `Sprints.tsx` (`[['sprints',pid],['tickets',pid]]`) and `ProjectList.tsx` (`[['tickets',pid]]`); **Board.tsx untouched** (keeps its presence/ghost-drag handler). Web typecheck + 18 unit tests + vite build green. **Browser-verified** (Keycloak SSO shares one session/profile, so tested by publishing to `agentpm:events` with a foreign `actorId` to simulate another viewer): List refetched `tickets` on a foreign-actor event + **dropped** a self-actor event (self-echo); Sprints refetched both `sprints` + `tickets`. No data writes, no console errors. | — |
| 2026-07-05 | P3.7 | **Milestone CRUD + WS event (3.7 R2).** Four routes in `routes/projects.ts` (reusing `loadProjectAuthorized`): `GET` (MEMBER, date asc) · `POST` (MEMBER, 201) · `PATCH` (MEMBER, partial name/description/date/done) · `DELETE` (ADMIN, 204). Cross-project guard via a local `loadMilestone` (404 when the milestone's `projectId` ≠ the authorized project). Every mutation fires `publishEvent('milestone.updated', { projectId, actorId })`. Added `'milestone.updated'` to the `WSEventType` union in `shared-types`; `Milestone` interface + `list/create/update/deleteMilestone` in web `api.ts`. New `milestones.test.ts` (4 tests): CRUD+ordering, MEMBER create/edit but MEMBER-can't-delete/OWNER-can, outsider 403, cross-project 404 → suite **69/69**; typechecks green; api restarted. | — |
| 2026-07-05 | P3.7 | **Schema foundation (3.7 R1).** One migration (`phase37_planning_surfaces`): new `Milestone` model (`projectId, name, description?, date, done`, cascade + `[projectId,date]` index), `Ticket.startDate?` + `Ticket.workstream` (new `Workstream` enum SPRINT/ADHOC, default SPRINT) + `[projectId,workstream]` index, and additive `TicketActivityType.WORKSTREAM_CHANGED` (its own `ALTER TYPE … ADD VALUE`). Rules centralized in `tickets.service.ts`: ADHOC⇒no sprint invariant (`ADHOC_SPRINT_CONFLICT` on create + ambiguous patch; assigning a sprint forces SPRINT, switching to ADHOC clears the sprint — both log `WORKSTREAM_CHANGED` alongside `SPRINT_CHANGED`), and `startDate ≤ dueDate` (`DATE_RANGE`) on create + update. Wired through zod (create/update/list-filter/import/batch), serializer (spread — auto), and web `Ticket` type + `Workstream`. 6 new API tests → suite **65/65**; api+web typechecks green. | — |
| 2026-07-04 | docs | **Opened Phase 3.7 — review-driven planning surfaces (R0).** First real-user review of the deployed product (positive; 8 asks) triaged into a full phase plan: gap analysis per ask, owner decisions (all AI → after Phase 5 with Beta-tag affordances; full interactive Gantt; real Milestone model; explicit sprint/ad-hoc `workstream` field), steps R0–R14 with sizes/sequencing/cut-order, deferred list, risks. New `phase-3.7-review-feedback.md`, indexed in the plan README. | — |
| 2026-07-04 | P3.3 | **Reports tab (3.3 R5, step 11 — Phase 3.3 done, R3 deferred).** New `/orgs/:slug/projects/:projectSlug/reports` (`ProjectReports.tsx`): hand-rolled inline SVG per the burndown precedent — **velocity bars** (value + sprint name + end date per completed sprint), **cycle & lead time** stat tiles (median/p85, i18n plurals) + weekly trend polylines (solid = cycle, dashed = lead, with legend), **workload** rows (avatar, proportional open-count bar, "N open · M in progress", unassigned bucket). Per-card empty states + loading skeletons. Nav: tree "Reports" leaf (BarChart3) per project + board-header link beside Sprints; `api.getProjectReports` + report types added. **Browser-verified** on live data: cycle tiles 7.3d from 1 closed ticket, workload bars w/ avatar + unassigned, velocity empty state; velocity-bar rendering exercised via a seeded demo project (session expired before the final screenshot — pending re-login). | — |
| 2026-07-04 | P3.3 | **Reports endpoints (3.3 R1/R2/R4, step 10).** New `services/reports.service.ts` (grouped-query style of `stats.service.ts`, reusable for the Phase-4 digest): **velocity** = last 12 COMPLETED sprints (`Sprint.velocity`), **cycle/lead** = reconstructed from `TicketActivity` STATUS_CHANGED rows over a 90-day window (lead = created→last DONE, cycle = first IN_PROGRESS→DONE with lead fallback; median + p85 + weekly trend buckets, Monday-UTC keys), **workload** = open tickets per member via one groupBy (IN_PROGRESS split out, unassigned bucket). Exposed as `GET /api/projects/:projectId/reports` (MEMBER). Test: full fixture through sprint-complete → velocity 8, closedCount 2, weekly sums, unassigned workload, outsider 403 — suite **59/59** *(count corrected 2026-07-04; an earlier row said 60)*. R3 cumulative-flow deferred; 3.3 doc → IN PROGRESS. | — |
| 2026-07-04 | basics | **Deeper search (missing-basics step 9).** `GET /api/search` now also matches ticket **descriptions** (title hits ranked above body hits via a stable client-side sort) and returns matching **projects** by name (membership-scoped, cap 5, `{ id, name, slug, key, orgSlug }`). ⌘K palette's "All projects" group lists project hits (folder icon + key + org slug → navigates to the board) above cross-org ticket hits. API test: description match + ranking, project hits, outsider gets `[]` — suite **59/59**. **Browser-verified:** palette "relationship" → project row → board. | — |
| 2026-07-04 | basics | **Label management (missing-basics step 8).** New `PATCH /api/labels/:id` (rename/recolor; org-scoped ADMIN; 409 on name clash) and the list endpoint now returns a `usageCount` per label (flattened Prisma `_count`). New `LabelsCard` on Org Settings: chip preview, per-label usage count, pencil→inline rename+recolor, two-step delete ("Remove from all tickets?"), create row; card is read-only for members (admin gating server-side too). `api.updateLabel` added. API test covers usage counts, rename/recolor reflecting on tickets, 409 clash, MEMBER 403 — suite **58/58**. **Browser-verified:** counts correct (1/2 tickets), rename Wed→Wednesday reflected on the ticket's chips. (Needed the usual `docker compose restart api` for the edited route to load.) | — |
| 2026-07-04 | basics | **Account page (missing-basics step 7).** New `/account` (`AccountSettings.tsx`): display name + avatar **URL** (no upload infra — URL only, `null` clears back to initials; `PATCH /api/me` schema now nullable), read-only email with "managed by your identity provider" hint, and an Appearance card (light/dark/system). `api.updateMe` added. Header email is now a link to the page with a live avatar chip; tree "My work" shows the avatar too; `initialsOf` extracted to `lib/utils` (mirrors the API helper). `useTheme` refactored from per-hook state to a tiny shared store (`useSyncExternalStore`) so the account-page control and the header toggle stay in sync. **Browser-verified:** save toast, avatar propagates header+tree instantly, persists across reload, theme buttons switch app-wide + sync the header icon. | — |
| 2026-07-04 | P3.6 | **i18n sweep (3.6 Part B, step 6).** Scripted orphan/missing diff of `en.json` against `t('…')` usage across `src`. Fixed the one genuine missing key — `board.clearFilters` rendered as a raw key on the list view (browser-verified fixed). Removed 6 true orphans (`common.any`, `landing.tagline`, `projects.backToOrgs`, `projects.title`, `drawer.description`, `sprints.removed`); confirmed the `onboard.*` / `theme.*` / `sprints.unit.*` groups are template-key usages, not orphans. Settings back-links normalized to sentence case. | — |
| 2026-07-04 | P3.6 | **Consistency + empty/loading states (3.6 Part B, step 5).** (1) Workspace tree gains gear **Settings** leaves at both levels — org (under Members) and project (under Board/Sprints); header link + ⋯ menu stay as secondary entries; browser-verified. (2) `OrgSettings`/`ProjectSettings` show skeleton cards while loading (was a blank-form flash); `TemplatesCard` gains a loading skeleton + a first-run hint explaining what templates do. (3) **subtasksDoneNudge verified end-to-end** via a new `workflow.test.ts` pipeline test (no fire while a sibling is open; exactly one `SUBTASKS_DONE` to the parent's audience on the last close; actor excluded) — API suite **56/56**. (4) 3.4 W3 doc reconciled: shipped trio documented, "IN_REVIEW → notify watchers" noted as dropped (watchers already get status-change notifications). | — |
| 2026-07-04 | P3.6 | **Notification per-type icons + labels (3.6 Part B, step 4).** `NotificationBell` gains a `TYPE_META` map: every `NotificationType` → lucide icon + i18n label (assigned → UserPlus, status change → RefreshCw, comment → MessageSquare, mention → AtSign, watching → Eye, sprint start/complete → Play/Flag, unblocked → Unlock, subtasks done → ListChecks); unknown future types fall back to the bell. Feed rows show the icon plus a "Label · relative-time" footer. **Browser-verified** (steps 3+4 together): bell feed renders icon + "Status change · 12h ago" per group; add-reaction "+🙂" visible without hover, picker opens, 👍 chip toggles on. | — |
| 2026-07-04 | P3.6 | **Touch-reachable comment reactions (3.6 Part B, step 3).** The add-reaction "+🙂" button in the ticket drawer was `opacity-0` until comment hover — unreachable on touch when a comment had no reactions yet. Now always visible at 50% opacity, full opacity on hover/focus/comment-hover. | — |
| 2026-07-04 | P3.6 | **CSV import round-trips labels + assignee (3.6 Part B, step 2).** `mapRows` maps a Labels column (`;`-separated, same join the export writes) and an Assignee column (aliases: assignee / assigned to / owner). `/api/tickets/import` resolves label names case-insensitively within the org and assignee by member email or exact display name — unknowns silently dropped so a half-matching file still lands. One lookup pass per file (labels + members preloaded), reuses `createTicket`'s `assertLabelsInOrg`/`assertOrgMember` checks. Sample CSV gains both columns. Web tests 18/18 (+label-split/alias cases), API workflow test resolves by email + by name + drops unknowns. FEATURES.md CSV note updated. | — |
| 2026-07-04 | P3.6 | **Test scaffold (3.6 Part B, step 1).** Web app finally has a test runner: Vitest + jsdom wired into `apps/web` (`test` block in `vite.config.ts`, `include: src/**/*.test.{ts,tsx}` so Playwright keeps `e2e/`), `"test": "vitest run"` runs under `pnpm turbo test`. 17 unit tests: `lib/csv.ts` quote/newline/CRLF round-trip, `mapRows` (sample-CSV round-trip, Jira header aliases, skip/cap/truncate, bad enum values → undefined), automation-toggle defaults, favorites store (persist/hydrate/corrupt-storage). Extracted the duplicated automation-defaults logic from `ProjectTools` into pure `lib/automationSettings.ts` (mirrors the server's `automationSettings()`). API side: +3 tests — `DELETE /api/orgs/:slug` OWNER-only (ADMIN 403 → 204 → 404), `DELETE /api/projects/:projectId` ADMIN-only (MEMBER 403), and the caller `role` field on `GET /api/orgs/:slug` (OWNER vs MEMBER). API suite **54/54**; web typecheck clean. | — |
| 2026-07-03 | docs | **Paused 3.5; opened Phase 3.6 (polish & hardening).** 3.5 H1/H2 (settings pages) shipped; H3 monitoring / H4 plan limits / H5 tokens+webhooks carried into 3.6 as A1–A3. New `phase-3.6-polish-hardening.md` also logs a **grounded loose-ends audit** of the whole 3.x arc: inconsistent settings entry points, uneven empty/loading states, CSV import dropping labels/assignee, W3-vs-plan reconcile + verify subtasks-done nudge, no per-type icons for the new notification types, hover-only comment reactions (no touch), i18n orphan/missing-key sweep, and — biggest gap — **no web test runner** + missing API tests for delete-org/project and the new `role` field. Suggested order: test scaffold first, then UX nits, edge cases, then A2→A1→A3. Indexed in README. | — |
| 2026-07-03 | P3.5 | **Platform hygiene H1+H2 — settings pages.** New `/orgs/:slug/settings` (`OrgSettings`): rename, workspace accent (moved out of Members), plan badge, OWNER-only danger zone. New `/orgs/:slug/projects/:projectSlug/settings` (`ProjectSettings`): rename, description, default branch (key shown immutable), ADMIN danger zone. Shared `DangerZone` component (type-the-exact-name-to-confirm). Role-gated: fields disabled / danger hidden for non-admins. Wired `api.deleteOrg`/`deleteProject`; entries added — org "Settings" link on the OrgProjects header, project "Settings" in the `ProjectMenu` ⋯. **Backend fix:** `GET /api/orgs/:slug` now returns the caller's `role` (only the list endpoint did before) — without it the settings page couldn't gate rename/delete. Browser-verified: both pages render, role gating correct, type-to-confirm arms exactly on match, project description save persisted across reload. | — |
| 2026-07-03 | UX | **CSV import — downloadable sample file.** Added a **Sample CSV** button to the list toolbar (and a "Sample CSV" action on the "no usable rows" error toast) that downloads `pmagent-import-sample.csv` — a 3-row example exercising the format on purpose: quoted comma, a multi-line acceptance-criteria cell, mixed Jira values ("To Do"/"Story"), empty optional cells. `SAMPLE_CSV_ROWS` + `mapRows` exported for testing. Verified live against served code: sample → `toCsv` → `parseCsv` → `mapRows` = 3 rows, 0 skipped, correct status/type/priority mapping, multiline AC preserved; download produces the file with the right name/MIME/header. | — |
| 2026-07-03 | UX | **Two-surface minimalist shell.** Header + workspace rail (and the collapsed strip) now sit on a quiet `bg-muted/40` tint (header adds `backdrop-blur` since it's sticky); the content area moved to the clean `bg-background`. Previously both were background-colored with a faint wash on content, so chrome and content were nearly indistinguishable. Hairline borders unchanged; works in light + dark via the existing tokens. | — |
| 2026-07-03 | fix | **Project cards fully clickable.** The OrgProjects card/row used an `absolute inset-0` overlay link, but every content block (title row, description, status bar, sprint chip, updated line) was also `position: relative` — later positioned siblings paint above the overlay, so those regions swallowed clicks and only the padding gaps navigated. Dropped `relative` from all non-interactive blocks (content now static → overlay on top everywhere); the favorite star + ⋯ menu keep `relative z-10` so they still win. Dashboard org cards audited — fine (fully wrapped in `Link`). | — |
| 2026-07-03 | UX | **In-app "Getting started" checklist + no-org dead-end fixes.** New `GettingStarted` card on the Dashboard: 6 steps along the app flow (create org → project → first ticket → move a card → invite a teammate → try ⌘K) with a progress bar; done steps derive from the org list's counts, "moved a card" and "opened palette" from one-time local flags set by the Board and CommandPalette. Each pending step links to the right place (the ⌘K step opens the palette); dismissible, auto-hides at 6/6, state persisted. **Dead-ends closed for brand-new accounts:** `/my-work` with zero orgs now shows a "Welcome to PMAgent" card with a Create-your-organization button (instead of two empty lists), and the sidebar's "No organizations yet" got a "+ Create your first organization" link → Dashboard. Browser-verified with a fresh registration: welcome card → CTA → Dashboard form + checklist; palette click → progress bump. Known nit: the two "did it once" flags are per-browser, not per-account. | — |
| 2026-07-03 | docs | **`FEATURES.md` — plain-language product guide**, restructured as **Part 1: an 8-step walkthrough along the app flow** (each step ends with "Next → …") + **Part 2: feature reference**. Covers everything shipped; "coming next" section closes it. Convention added to `CLAUDE.md`: update it in the same commit whenever a user-facing feature ships. | — |
| 2026-07-03 | P3.4 | **Workflow & automation W1–W4 complete (W5 recurring deferred).** One migration (`20260703060139_workflow_automation`): `TicketTemplate` model, `Project.automation` JSON, `TICKET_UNBLOCKED`/`SUBTASKS_DONE` notification types. **W1 templates:** CRUD + `seed-defaults` endpoints (read MEMBER / write ADMIN), Bug+Feature starters seeded on org create, "New from template" dropdown on the Board header (creates + opens drawer), management card on Members with backfill button for pre-existing orgs. **W2 unblock nudge:** closing a ticket emits `ticket.unblocked` for each dependent whose last open blocker it was → notification to that ticket's audience. **W3 toggles** (Zap menu on Board, merged PATCH on project): unblockNudge (default on), autoTodoOnAssign (assign moves BACKLOG→TODO inside the update tx), subtasksDoneNudge. **W4 CSV:** client export of the filtered list; import with Jira header aliases (Summary/Issue Type/…), status/type value mapping, preview-then-commit, `POST /tickets/batch`-style `/tickets/import` endpoint (cap 500, per-row create service so numbering/watchers/activity stay correct). API suite 51/51 (+6 in `workflow.test.ts`; nudge tests connect Redis like realtime). Browser-verified: seed→template→BUG ticket w/ spec, auto-TODO on assign, CSV buttons. Committed locally — **not pushed** (per new rule). | — |
| 2026-07-02 | UX | **Ticket drawer restructure — sticky header, grouped sections, expandable width.** Header (key · viewers · editable title · status/priority/**type** dropdowns — type is newly editable in the drawer) is now `sticky` inside the sheet's scroll container, so it stays visible while scrolling; built-in sheet padding/close replaced (own ✕ + width toggle in the header). Body regrouped into collapsible bordered sections with persisted open state (`agentpm-drawer-sections`): **Details** (assignee/points/due/sprint, open by default, collapsed shows "assignee · sprint" summary), **People & labels** (watchers + labels merged, collapsed by default with 👁/🏷 counts), **Relationships** (collapsed by default, shows a red Blocked chip when blocked). **Expandable width:** Maximize/Minimize toggle (persisted `agentpm-drawer-wide`, hidden on mobile) grows the sheet 36rem → min(72rem, 92vw); content realigns to two columns on `lg` — spec + conversation left, meta sidebar (300px) right; Details grid drops to one column in the sidebar. Browser-verified: sticky while scrolled, 576→1152px toggle, two-column realign, section toggle + persistence, mobile 375px stack. | — |
| 2026-07-02 | fix/UX | **Comments 500 root-caused + fixed; slim theme-aware scrollbars.** Comments broke because the api **container's** Prisma client was stale after the reactions migration (host `prisma generate` doesn't reach the image's `node_modules`) → `Unknown field 'reactions'` 500s; fixed by generating inside the container + restart, gotcha added to Now/Next notes. New `.scrollbar-slim` utility (thin, `muted-foreground`-tinted thumb, transparent track, WebKit + Firefox) applied to board columns, list table, drawer sheet, tree rail, ⌘K list, relations picker — kills the stark white OS scrollbar in dark mode. Refreshed the stale **Now/Next/Blocked** header (was still pre-Phase-3). Re-verified in-browser: comment posts + renders markdown bold, mention picker → highlighted chip, 👍 reaction chip toggles, slim scrollbar on board. | — |
| 2026-07-02 | P3.2 | **Collaboration C1–C3 complete (C4 attachments deferred).** Audit: mention autocomplete (C1) + markdown rendering (C2) already existed from Phase 2.1 — added the missing polish: mentions now render as highlighted `@Name` chips (styled span injected pre-markdown; DOMPurify keeps class). **C3 reactions built end-to-end:** `CommentReaction` model + migration `20260702150624_comment_reactions` (unique per comment+user+emoji), `POST/DELETE /tickets/:id/comments/:commentId/reactions` (fixed set 👍 🎉 👀 ❤️, comment-in-ticket check), reactions included on the comments list, chip UI under comments in both the Comments and Story tabs (count + mine-highlight, toggle, hover "+🙂" picker). API 45/45 (+1); web tsc green. | — |
| 2026-07-02 | P3.1 | **PM depth — step 6 (final): bulk actions. Phase 3.1 COMPLETE.** New `BulkBar` (floating bottom bar): status / assign / sprint / add-label menus + two-step archive, one `POST /tickets/batch` per action, invalidates ticket + my-work queries. Board cards grow a corner hover-checkbox (drag-safe via stopPropagation); list gets a checkbox column with select-all; selection clears on project change. Fixed list-view filter i18n keys (`board.search`/`priorityAny`/… — raw keys were rendering). **Browser-verified end-to-end** on the dev stack (fresh user → org → project → 6 tickets): dependency add via drawer → red Blocked badge on card+row; blocker → DONE clears it; bulk-archived 3 duplicates; bulk status → To Do; ⌘K "All projects" search hit; `/my-work` assigned/watching; mobile 375px + desktop 1440px, board⇄list toggle persisted. | — |
| 2026-07-02 | P3.1 | **PM depth — step 5: ⌘K global search + My-work.** Palette: 250ms-debounced `GET /api/search` on the root page (≥2 chars) → "All projects" group (deduped against the current project's tickets, shows project slug, navigates cross-org). New `serializeTicketWithOrg` on the API so search/my-work hits carry `orgSlug`/`projectSlug` for links (`TicketHit` type on the web). New `/my-work` page — "Assigned to me" + "Watching" sections with counts, blocked pills, priority/status/project columns, skeletons, empty states — linked from the tree rail ("My work" at the top). i18n `mywork.*`, `tree.myWork`, `palette.everywhere`. API+web tsc green; 9 affected tests pass. | — |
| 2026-07-02 | P3.1 | **PM depth — step 4: blocked badge on board cards.** `TicketCardBody` shows a red `Ban`+"Blocked" pill next to priority when `blockedBy > 0` (tooltip = open-blocker count); list rows already had it from step 2. Dark-mode variants included. | — |
| 2026-07-02 | P3.1 | **PM depth — step 3: relationships panel in the drawer.** New `RelationsSection` (mounted between Labels and Spec): parent (set/clear), subtasks (with done-count, add = set candidate's `parentId`), blocked-by / blocks (add/remove via the dependency endpoints). Searchable ticket picker dropdown (excludes already-linked), done/cancelled refs struck through, badge click navigates staying in board vs list view. Mutations invalidate relations + ticket lists so blocked badges stay fresh; API cycle errors surface as toasts. i18n `relations.*`. | — |
| 2026-07-02 | P3.1 | **PM depth — step 2: list/table view + board⇄list toggle.** New `ProjectList.tsx` at `…/projects/:projectSlug/list` (+ `/list/ticket/:number` for the drawer): filter bar (search/status/priority/type/assignee/sprint/label), sortable Key/Priority/Updated headers (server-side sort), blocked pill + label chips in the title cell, skeleton rows, empty states, row click opens the existing `TicketDrawer`. New `ViewToggle` segmented control on both Board and List headers (last view persisted to `agentpm-project-view`). i18n `list.*`. Web tsc + build green. | — |
| 2026-07-02 | docs | **Phases 3.1–3.5 planned** — the "feature-rich & simple" track slotted between deploy (3) and channels/agents (4+): 3.1 PM depth (in progress), 3.2 collaboration (mentions UI/markdown/reactions/attachments), 3.3 insights & reporting, 3.4 simple workflow & automation, 3.5 platform hygiene (settings UI, monitoring, plan limits, API tokens+webhooks as the Phase-5 bridge). Indexed in the plan README. | — |
| 2026-07-02 | P3.1 | **PM depth — step 1: relations/search/my-work/batch backend.** New `services/relations.service.ts` (`getRelations`, `addDependency`/`removeDependency` with self+cycle guards, `blockedByCounts`). Ticket list now returns `blockedBy` (count of incomplete blockers, one grouped query). `PATCH /tickets/:id` accepts `parentId` (same-project + ancestor-walk cycle guard). New endpoints: `GET /tickets/:id/relations`, `POST/DELETE /tickets/:id/dependencies`, `POST /tickets/batch` (single-org gate, label org check, transaction-per-ticket with activity/events), `GET /api/search` (cross-org, membership-scoped, KEY-N aware), `GET /api/me/work` (assigned vs watching). Types + client methods mirrored in `web/src/lib/api.ts`. API suite 44/44 (+6). On `dev`. | — |
| 2026-07-01 | UX | **Org/Project redesign — step 6: activity feed + context menus.** New `components/ActivityFeed` (recent `TicketActivity` on the OrgProjects overview: actor avatar + humanized action + `KEY-N` ticket link + relative time; i18n `activity.*`) fed by `api.orgActivity`. New `components/ProjectMenu` (`⋯` `dropdown-menu` on project cards/rows: Board / Sprints / Members nav, favorite toggle, copy key). Full API suite 38/38, web build + typecheck green. Completes the redesign (A+B+C). On `dev`. | — |
| 2026-07-01 | UX | **Org/Project redesign — steps 4+5: Dashboard + OrgProjects screens.** `Dashboard` (orgs) is now enriched cards (accent dot, plan + role pills, `MetricChip`s for projects/members/open) or dense rows, behind a persisted `DensityToggle`. `OrgProjects` gains an org overview header (4 metric chips from `getOrg` `stats`, member `AvatarStack`, pending-invite hint, members link) and project cards/rows: key badge, description, `StatusBar` (to do/in progress/done), active-sprint chip (name · %done · days-left via `daysUntil`) or "no active sprint", `updatedAt` relative time, favorite star (float-to-top). Create form upgraded to name + optional KEY + description. Added `time.daysUntil`; i18n `dashboard.*`/`projects.*`. Verified in preview: grid+list, desktop+mobile, light (shell dark verified in step 3). On `dev`. | — |
| 2026-07-01 | UX | **Org/Project redesign — step 3: tree rail + shell + breadcrumbs.** New `components/OrgTree` (persistent Org→Project nav: accent dot per org, key badge + open-count pill + favorite star per project, Board/Sprints/Members leaves; favorites float to top; expansion persisted to `localStorage`, active org/project auto-expand) and `components/Breadcrumbs` (header trail from cached queries, md+). `Layout` restructured into a sticky-header + two-pane shell: desktop `lg+` shows a collapsible 256px rail (collapse state persisted), below `lg` it hides behind a hamburger that opens the tree in a left `Sheet`. Driven by the existing `['orgs']`/`['projects', orgId]` caches — no extra fetching. i18n `nav.*`/`tree.*`/`breadcrumb.*`. Verified in isolated preview: desktop rail (dark+light), mobile Sheet drill-down. On `dev`. | — |
| 2026-07-01 | UX | **Org/Project redesign — step 2: shared FE components.** `components/MetricChip` (stat tile), `components/StatusBar` (3-bucket segmented mini-bar from `byStatus`), `components/AvatarStack` (overlapping avatars + `+N`), `components/DensityToggle` (grid⇄list, lucide icons). Libs: `lib/useLocalStorageState` (JSON-backed UI-pref hook) and `lib/favorites` (cross-component favorited-project store via `useSyncExternalStore`, localStorage-persisted). Web typecheck green. On `dev`. | — |
| 2026-07-01 | UX | **Org/Project redesign — step 1: backend aggregates + activity.** New `services/stats.service.ts` (`orgListStats`, `projectListStats` — grouped queries, no N+1, mirroring `sprintCounts`) and `services/activity.service.ts` (`recentActivity` + serializer). `GET /api/orgs` now carries `projectCount`/`memberCount`/`openTicketCount`; `GET /api/orgs/:slug` adds `stats` + `membersPreview` + `pendingInviteCount`; `GET /api/projects` adds `openTicketCount`/`byStatus`/`activeSprint`. New `GET /api/orgs/:slug/activity` + `GET /api/projects/:projectId/activity` (recent `TicketActivity`, membership-gated). Types mirrored in `web/src/lib/api.ts` (`OrgDetail`, `ActiveSprintSummary`, `ActivityItem`, enriched `Project`/`Organization`). No migration (all data already modeled). API suite 38/38 (+3). On `dev`. | — |
| 2026-07-01 | UX | **Landing (login) parallax backdrop + product tagline.** New `ParallaxBackground` component: theme-token base wash + three translucent colored glows that drift with the cursor at different depths on desktop (fine pointer) and slow-float everywhere (touch/mobile), plus a masked grid; honors `prefers-reduced-motion`. Landing restructured into a responsive two-column layout — brand + product-defining headline/subtitle (`landing.headline`/`subtitle`) on the left, translucent `backdrop-blur` sign-in card on the right; stacks to one centered column on mobile. Verified in-browser: desktop two-column, mobile stacked, light + dark. On `dev` branch. | — |
| 2026-07-01 | UX | **Loading skeletons for the three fetch lists.** Dashboard (orgs) and OrgProjects (projects) rendered an empty bordered `<ul>` while loading (no skeleton) — now show 3 pulsing `Skeleton` rows on `isPending`, with the empty-state gated behind loaded data. Board already had `BoardSkeleton` but only on `tickets.isLoading`, which skips the org→project resolution phase (tickets query is `enabled: Boolean(projectId)` = disabled → `isLoading` false → the "first ticket" empty state flashed instead); switched the guard to `!tickets.data` so the skeleton covers the whole load. On `dev` branch. | — |
| 2026-07-01 | P3 | **CI/CD pipeline** — `.github/workflows/deploy.yml`: push to `main` → **test** (lint/typecheck/vitest on Node 22 + pg/redis service containers) → **build+push** `agentpm-api`/`agentpm-web` to GHCR (web bakes prod `VITE_*`; SHA + latest tags, gha cache) → **deploy** via SSH to the EC2 box (`docker compose pull` by SHA → migrate → `up -d`). Needs repo secrets `EC2_HOST`/`EC2_USER`/`EC2_SSH_KEY` (= pmagent.pem) + the 2 GHCR packages made public. | — |
| 2026-06-30 | P3 | **First prod deploy LIVE on AWS EC2** — `pmagent.sanjaykumarp.info` (app/api/auth) on a t3.medium (Amazon Linux 2023), self-hosted data, Caddy auto-TLS (Let's Encrypt). Login/register/logout verified in-browser. Three runtime fixes surfaced: **(1)** api `Dockerfile` runner copied prod-deps + a non-existent `node_modules/.prisma` (broke on the pnpm workspace layout) → now copies the resolved tree (`node_modules` + `apps/api` + `packages`) from the `build` stage. **(2)** `prod-bootstrap.sh` client update failed atomically on the dotted `attributes.post.logout…` `-s` key → split into URL `-s` fields + an attributes `-f -` JSON merge (post-logout + pkce). **(3)** prod Keycloak emitted `http://auth…:8080` in its OIDC metadata (bare `KC_HOSTNAME`) → API couldn't fetch JWKS → every call 401'd after ~5s + CORS errors; fixed with `KC_HOSTNAME=https://${AUTH_DOMAIN}` (full URL incl. scheme). Box prep: installed docker/compose/buildx (AL2023 ships none/old) + 2 GB swap; opened SG 80/443. | — |
| 2026-06-27 | P3 | **Phase 3 deploy artifacts.** `docker-compose.prod.yml` (Caddy auto-HTTPS + prod Keycloak `start --import-realm` + restart policies + prod `keycloak-init`), `infra/caddy/Caddyfile` (env-driven domains), `infra/keycloak/prod-bootstrap.sh` (points the agentpm-web client at the prod domain, wires social IdPs, disables first-broker Review Profile), `.env.prod.example`. Compose merge validated. Targets `pmagent.sanjaykumarp.info` on EC2 (self-hosted data). Next: CI/CD workflows + first deploy. | — |
| 2026-06-27 | docs | **Deploy config reference (Phase 2.9.1)** — new `phases/phase-2.9.1-deploy-config.md`: the two env configs (dev `.env` vs prod `.env.prod`) with every variable, the infra-to-provision list, and a mandatory-vs-deferrable breakdown across local dev / shared testing deploy / prod. Flags the dev-only prod-config gaps (realm `localhost` URLs, IdP + Keycloak `kcadm` bootstrap, `VITE_*` build-time) for Phase 3 to close. Indexed in README. | — |
| 2026-06-27 | P2.8 | **Keycloak login/register theme → matches the app.** Restyled the `pmagent` theme to the app's Landing card (tokens, app-style inputs, integrated password toggle [removed PatternFly `::after` borders], no grey footer band, tidy register layout via Bootstrap-col reset); inline themed PMAgent wordmark; **‹ Back** link on both pages → the app (`agentpm-web` client `baseUrl` set in realm + applied via kcadm). **Light/dark/system uniformity:** `lib/theme.ts` mirrors the app theme into a shared `pmagent-theme` cookie; the theme's early `<head>` script applies `.dark` (OS fallback) pre-paint; CSS mirrors the app's light/dark token palette. New `template.ftl` (override of KC 26.0.8 base). Web typecheck green; browser-verified light + dark. Caveats: template pinned to KC 26.0.8; prod cookie-domain two-label heuristic. | — |
| 2026-06-26 | docs | **Social-login setup runbook** — new `references/13-social-login-setup.md`: step-by-step to register Google/Microsoft/GitHub OAuth apps (consent screen, redirect/callback URIs → Keycloak broker endpoints, where to copy client id/secret), apply via `keycloak-init`, + prod/security notes. Linked from the README references index, the Phase 2.8.5 prerequisites, and this tracker. | — |
| 2026-06-26 | P2.8.5 | **Auth UX — in-app OAuth (frontend + IdP plumbing).** `lib/auth.ts` `loginWith(idp)` via `keycloak.login({ idpHint })`; `Landing.tsx` redesigned — Google/Microsoft/GitHub buttons (`ProviderIcons`) + email/password → branded KC page + PMAgent logo + i18n. `keycloak-init` extended: kcadm upsert of google/microsoft/github IdPs from env (skip w/o creds) + disable first-broker "Review Profile" (seamless auto-create); `GOOGLE_/MICROSOFT_/GITHUB_CLIENT_*` added to `.env`/`.env.example` (empty). Email/password = **2.8.5b hybrid**. Web typecheck/build green; `keycloak-init` exit 0. Social round-trip pending user-registered OAuth apps. | — |
| 2026-06-26 | plan | **Phase 2.8.5 (auth UX) drafted.** New `phases/phase-2.8.5-auth-ux.md`: in-app **Google/Microsoft/GitHub** sign-in via Keycloak `idpHint` (no hosted login page; backend unchanged), seamless first-broker auto-create, IdP secrets injected at runtime; email/password kept (open sub-decision: branded KC page vs custom ROPC). Wired into README build-flow + index and this tracker; sits in the Keycloak line before Phase 3. Plan only — **awaiting user go-ahead** to implement. | — |
| 2026-06-26 | P2.8 | **Branding → PMAgent (implemented).** Web: `index.html` title + `favicon.svg`, `en.json` `common.appName`/`invite.title`, Swagger `title` → PMAgent; E2E assertion `/PMAgent/i`. Keycloak **Tier 2**: new `infra/keycloak/themes/pmagent/login` (`theme.properties` + `pmagent.css` + wordmark `logo.svg`), mounted into the KC container via compose; realm `displayName`/`displayNameHtml`=PMAgent + `loginTheme`=pmagent + client display names; applied to the running realm via `kcadm`. Login page verified over HTTP (theme css + logo → 200). Identifiers (`@agentpm/*`, realm/client ids, `agentpm.io`) intentionally unchanged. **35 API tests green** (host), web typecheck + build green. | — |
| 2026-06-26 | plan | **Phase 2.8 (branding → pmagent) drafted.** New `phases/phase-2.8-branding.md`: rename the product's public face from "AgentPM" to **pmagent** across web (title, `app.appName`, invite copy, Swagger) + the Keycloak sign-in (`realm-agentpm.json` displayName) — **display only**; identifiers (`@agentpm/*`, realm/client ids, `agentpm.io`) explicitly out of scope. Wired into README build-flow + index and this tracker; sequenced before Phase 3. Plan only, not implemented. | — |
| 2026-06-26 | plan | **Phase renumber + index refresh:** `2F` → **Phase 2.1** (renamed `phase-2.1-gap-closure.md`; it patches Phase 2, so it sits right after it) + marked ✅ COMPLETE (file previously still read draft/not-started). **Phase 2.7** kept parked but resequenced into the **Phase 5** agent block — folded A2/A3/A4 into Phase 5's deliverables and removed the floating 2.7 section that sat between 2.6 and Phase 3. Refreshed `agentpm-plan/README.md` build-flow + phases index (added 2.1/2.6/2.7, previously missing). Docs only, no code. | — |
| 2026-06-25 | P2.6 | **Two-user realtime verification** (2nd user in incognito Chrome): ✅ **E1** presence avatar appears when both view EMPL-1; ✅ **B1** ghost-drag card shows while the other user drags; ✅ **E3** multiple changes by the other user group into one "N updates" row in the watcher's bell. Confirms the ephemeral WS relay works cross-session. | — |
| 2026-06-25 | P2.6 | **Browser verification (Slices 4–6) in Chrome:** ✅ G2 theme `t` (light→dark), G2 accent (green preset → `--primary` recolor + persist + Reset), C2 AC checkboxes (1/3→2/3 toggle persisted), F2 drag EMPL-5 → Sprint 1 (2→3 tickets), F1 burndown (ideal+actual on completed sprints); no WS console errors. **Deferred:** E1/B1/E3 (since two-user verified ↑), B3 mobile, B5/H2 (specific data) — build/test-verified. | — |
| 2026-06-25 | P2.6 | **Slice 6 (backend, G2/F1) — finishes non-agent 2.6.** G2: migration `20260625000000_org_accent` (Organization.accentColor, applied via `docker compose exec api … prisma migrate deploy` + generate, host+container); PATCH `/orgs/:slug` accepts accentColor; `lib/accent.ts` hex→HSL → `--primary`/`--ring` in Layout (org-scoped, cleared off); `lib/theme.ts` light/dark/system tristate (live OS follow) + `t` shortcut + sun/moon/monitor toggle; accent picker on Members. F1: `GET /sprints/:id/burndown` reconstructs remaining-work/day from STATUS_CHANGED→DONE activity (points or ticket count) vs ideal; `BurndownSparkline` SVG on active/completed sprints. +i18n. **35 API tests green**; typecheck/build green. | 3be46e4 |
| 2026-06-25 | P2.6 | **Slice 5 (realtime, E1/B1):** ephemeral WS relays. shared-types +`ticket.presence`/`ticket.drag`. ws-server: post-auth handles `ticket.viewing` (tracks `socketTicket`, broadcasts `ticket.presence{byTicket}`) and `ticket.drag` (relays w/ sender `actorId`); close clears + re-broadcasts. websocket.ts hook returns `{send}`. Board sends viewing on drawer open/close + drag on start/over/end; renders pulsing viewer avatars (E1) on card + drawer header and faint ghost cards (B1) in target column. API **35 tests green** (no WS regression); typecheck+build green. Browser-verify pending (two sessions + re-login). | 2fc0ac2 |
| 2026-06-25 | P2.6 | **Slice 4 (frontend, C2/E3/B3/F2):** C2 `lib/checklist.ts` parse/toggle — AC `- [ ]` lines render as interactive checkboxes (toggle rewrites AC text via patch) + N/total; E3 NotificationBell groups by ticket ("N updates", per-group unread dot, "N unread since you last looked" header, group-click marks all read); B3 TicketCard touch swipe → next/prev status (composes with dnd touch listener, swipedRef suppresses click); F2 Sprints page DndContext — draggable `TicketChip`, droppable `BacklogZone` + sprint cards → add/removeFromSprint. +i18n. typecheck+build green; **browser-verify pending re-login** (KC token expired). Also parked agent-first A2–A4 → Phase 2.7 (commit 1043152). | 3998063 |
| 2026-06-25 | P2.6 | **Slice 3 (delight grab-bag, 7 items):** B2 `WIP_LIMITS` + amber motion-safe pulse on count badge; B5 `staleBorderClass` left-border time-decay; C1 third "Story" drawer tab merging comments+activity chronologically; C3 slash commands in comment box (`/status /assign /sprint /due /label` → `patch`, else posts as comment) + `/`-triggered command menu; F3 sprint capacity bar (committed pts vs last velocity, amber overcommit); H1 guided empty-board starter (create in Backlog → open drawer); H2 team-of-one invite nudge on Members. +i18n. **Browser-verified:** C3 `/status done` moved EMPL-4→Done, C1 story tab, B2 N/limit badges, H1 starter card. typecheck+build green. _(Note: long verify session expired the KC token at the end — re-login needed; not a code issue.)_ | 8d6bca3 |
| 2026-06-25 | P2.6 | **Slice 2 (palette power, D1/D2/D3):** CommandPalette rewrite with cmdk sub-pages — open-ticket actions (status/assign/sprint/label via `patchOpen`→updateTicket+invalidate+toast+close; Backspace-on-empty steps back) + global theme toggle (D1). `lib/frecency.ts` localStorage count+recency score; Board records ticket/project visits; palette "Recent" group when query empty (D2). `lib/parseQuickCreate.ts` parses `Title !high @user #sprint` → priority/assignee/sprint, unresolved tokens kept in title; create item shows parsed badges (D3). +palette.* i18n. **Browser-verified:** D3 created EMPL-4 w/ HIGH/Adish/Sprint2; D1 status→In Progress patched+closed; D2 Recent showed EMPL-4. typecheck+build green. | c8981d1 |
| 2026-06-25 | P2.6 | **Slice 1 browser-verified** in Chrome vs live stack: A1 ring 1/3→2/3 on save + goal/constraints editable; E2 Undo reverts (LOW→Undo→HIGH, confirmed via DOM observer); G1 confetti canvas injected on →DONE; G4 `?` overlay opens; C4 "18h ago" + exact hover `title`; B4 focus dims non-mine cards. G3 build-verified (load <1s). Also confirmed 2.1 C9 silent-SSO restores session on a fresh tab. Fixed a Vite stale-transform crash (`cn` undefined) by restarting the web container. _(no code change beyond Slice 1)_ | 2a56e74 |
| 2026-06-25 | P2.6 | **Slice 1 (7 quick wins, web-only):** A1 `ReadinessRing` (card + drawer) over goal/AC/constraints + drawer now edits goal/constraints (new "Spec" block); E2 toast→Undo — `applyMove` narrates + offers undo on column change only (silent on reorder), drawer `patch` builds `inverseInput` for undo; G1 `lib/confetti.ts` dependency-free canvas burst on move→DONE (board + drawer), no-op under reduced-motion; G3 `BoardSkeleton` mirrors real columns/cards; G4 `KeyboardHelp` radix-dialog overlay on `?`; C4 `lib/time.ts` + `RelativeTime` in drawer comments/activity + notification bell; B4 focus mode (`f` key + header toggle) dims non-mine cards via `focusUserId` thread. +`goal`/`constraints` on web `UpdateTicketInput`; i18n strings added. typecheck + build green; browser-verified. | 2a56e74 |
| 2026-06-24 | P2.5 | Stage 2.5B (i18n): `react-i18next` + `i18next-browser-languagedetector` + `lib/i18n.ts`; `locales/en.json` baseline; **externalized every UI string** across Landing/Layout/Dashboard/OrgProjects/Members/InviteAccept/Board/Column/TicketCard/TicketDrawer/Sprints/NotificationBell (incl. toasts, placeholders, empty states); localStorage persistence. Rebuilt web container for new deps. typecheck/build green. | 72b5ca6 |
| 2026-06-24 | P2.5 | Stage 2.5E (a11y + Playwright): a11y — ticket cards keyboard-activatable (Enter/Space) + focus ring + `aria-label`, bell `aria-label`, radix dialogs already trap/restore focus. Playwright scaffold — `playwright.config.ts`, `e2e/global-setup.ts` (KC UI login → storageState; SPA check-sso restores from cookie), `e2e/core-flow.spec.ts` (org→project→add/open ticket→comment + optional cross-user mention via password-grant API), `test:e2e` script, `.gitignore`. Runs locally (needs `playwright install` + seeded user + stack); **not executed in sandbox**. typecheck/build green. | 7cb3585 |
| 2026-06-24 | P2.5 | Stage 2.5D (Cmd-K): `cmdk` + `components/ui/command.tsx`; `CommandPalette` (⌘/Ctrl-K) mounted in Layout — quick-create ticket from the query, jump to ticket by #/title, switch project/org; context derived from URL. typecheck/build green. (Docker deps-layer cache wouldn't rebust for the new dep amid a Docker Hub DNS blip → installed cmdk into the running container to verify; committed lockfile bakes it in on next clean image build.) | 8d6ed00 |
| 2026-06-24 | P2.5 | Stage 2.5C (mobile): board `snap-x` scroll + columns `w-[85vw]` (sm:w-72); dnd sensors → Mouse(5px)+Touch(long-press 220ms, so swipe still scrolls)+Keyboard(a11y reorder); removed `touch-none` on cards; Layout header hides email under `sm`. typecheck/build green. | c55805c |
| 2026-06-24 | P2.5 | Stage 2.5A (dark mode): tailwind `darkMode:'class'` + `.dark` CSS-var palette; `theme.ts` (localStorage + `prefers-color-scheme`, applied pre-render); sun/moon toggle in Layout; sonner Toaster `theme=system`; retrofit Landing/Dashboard/OrgProjects hard-coded light colors → tokens. typecheck/build green. | f29497f |
| 2026-06-24 | P2.1 | Stage 2.1 (gap closure, all 11): **A** — sprint picker in drawer, per-card status menu (hover ⋯), delete-ticket in drawer, board search/filter/sort bar, **Members & invites page** (add-by-email + create/copy/revoke invite links), sprint↔tickets on Sprints page + move-between-sprints. **B** — labels: `routes/labels.ts` CRUD (org-scoped) + assignment via `PATCH /tickets/:id` `labelIds` (replace-set, cross-scope guard) + drawer picker; @mention picker (editor shows `@Name`, sends `@[uuid]`). **C** — Keycloak `check-sso` + `public/silent-check-sso.html` (refresh keeps session); within-column reorder (`useSortable`/`SortableContext`, fractional `positionBetween`); drawer optimistic updates. +2 API tests (label assign/cross-scope, body-less DELETE). **35 API tests** + typecheck/build green. Browser-verify pending (C9 cookies, C10 reorder feel, mention→notify). | 3db2df1 |
| 2026-06-24 | P2 fix | Body-less requests 400'd (`Body cannot be empty when content-type is application/json`) — broke DELETE watcher / delete ticket / remove-from-sprint and body-less POSTs (start/complete sprint, mark-read). Fix: web `request()` omits `Content-Type` when there's no body; api adds a tolerant `application/json` parser (empty → undefined). +1 regression test (DELETE watcher w/ json content-type → 204). 34 tests green. | 3db2df1 |
| 2026-06-24 | plan | Phase 2.1 (then "2F") gap-closure draft: 11 gaps between the Phase-2 plan (drawer/board/UX/DoD) and 2A–2E, found in in-browser verification; grouped A (UI over existing APIs) / B (new backend) / C (polish), with approach + effort per item. | f37a9e9 |
| 2026-06-24 | P2/E | Stage 2E (board/drawer/sprints/bell + **Phase 2 complete**): Kanban `Board` (dnd-kit drag→status+position, quick-add per column, JIRA status dropdown, completion bar, presence avatars), `TicketCard`/`Column`, `TicketDrawer` (deep-link `/ticket/:number`; title/desc/AC edit, status+priority, assignee picker, watcher chips, story points, due date, Comments\|Activity tabs, marked+DOMPurify markdown), `NotificationBell` (WS-live unread badge + deep-link), `Sprints` (create/start/complete + completion bars). shadcn ui added: sheet/dropdown-menu/tabs/textarea/skeleton/label; deps dnd-kit/radix/marked/dompurify/sonner; Toaster mounted; full-width layout. API client extended (tickets/sprints/notifications/comments/watchers/activity). Backend: ticket-create accepts `status` (quick-add into column); **`MAX_LIMIT` 100→200** (board fetches whole project — was silently 400ing the board). +4 API tests (comments/watchers/cross-scope/update). **Verified in Chrome** (create/list/drag/status/drawer/sprint). 33 tests + web build green. | 36ee154 |
| 2026-06-24 | P2/D | Stage 2D (frontend foundation): shadcn/ui infra (`components.json`, `lib/utils.ts` cn, `@/*` paths in web tsconfig, tailwind theme tokens + `index.css` CSS vars, base ui: button/input/card/badge/avatar; deps cva/clsx/tailwind-merge/tailwindcss-animate/lucide-react/radix slot+avatar + `@agentpm/shared-types`). Routing restructure in `App.tsx` (always-on Router; public `/invite/:token`; gated via `RequireAuth`→Landing). `pages/InviteAccept.tsx` (unauth → sign-in-to-accept returns to token; authed → auto-accept → redirect to org). Backend `GET /orgs/:slug/members` enhanced (+`avatarUrl`, `initials` fallback). `lib/api.ts` +Member/Invite types +`listMembers`/`createInvite`/`acceptInvite`. `lib/websocket.ts` `useProjectWebSocket` (refresh-before-connect, backoff reconnect, refetch-on-reconnect, self-echo dedupe) on shared `WSMessage`. Layout sign-out → Button. Verified: web typecheck + vite build, api typecheck + 29 tests; full docker stack healthy (in-browser confirmed). | aec19c5 |
| 2026-06-24 | P2/C | Stage 2C (sprints + realtime + notifications + invites, backend): `routes/sprints.ts` (CRUD, start/complete+velocity, add/remove tickets w/ cross-scope guard, completion counts via groupBy); `websocket/ws-server.ts` (`/ws` handshake: auth-timeout→4001, `auth/verify-token.ts` shared jose JWKS verifier, project-membership gate, project+user rooms, presence, fan-out by projectId/userId); `services/notifications.service.ts` (subscribe ticket.* → recipients assignee/creator/watchers/@mentioned − actor → `Notification` rows + `notification.new`); org invites (CSPRNG token, role-cap, single-use, expiry) on org routes + `routes/invites.ts` accept; `routes/notifications.ts` caller-scoped (IDOR-safe). Event bus refactored to single Redis subscription → multi-handler dispatch; wired in `buildServer` + `onClose` dispose. Shared `WSMessage`/`WSEventType`. +10 tests (sprints, invites single-use/expiry, notification IDOR, WS timeout/auth/delivery). Verified: typecheck, build, **29 tests**. | 25c278d |
| 2026-06-24 | P2/B | Stage 2B (tickets backend): `routes/tickets.ts` (CRUD + soft-delete, status quick-change, comments, watchers, activity, list) via `fastify-type-provider-zod`; `tickets.service.ts` — transactional create + atomic numbering (`Project.ticketCounter`), `updateTicket` writing `TicketActivity` + returning post-commit events, cross-scope validation (assignee/labels/sprint/parent/deps); cursor pagination helper (Prisma keyset, id tiebreaker); lazy Redis `event-bus.ts` (`publishEvent` no-op until 2C); Swagger `/documentation`; `/ready` + SIGTERM/SIGINT graceful shutdown. +6 ticket API tests (numbering, RBAC 403, assign/activity, cross-scope 400, pagination round-trip, soft-delete). Verified: db:generate, typecheck, build, **19 tests**. | ac32a7e |
| 2026-06-24 | P2/A | Stage 2A (data): Phase-2 Prisma schema (Ticket/Sprint/Label/Comment/TicketDependency/TicketWatcher/TicketActivity/OrgInvite/in-app Notification + enums; agent scalar cols kept, agent tables deferred); `Project.key`+`ticketCounter`; onDelete clauses; Label↔Org relation. Hand-written migration with `key` backfill (existing projects → WEBA/EMPL). Project-create derives+dedupes key. Idempotent `db:seed`. Test truncation extended to new tables. Verified: migrate deploy, prisma generate, typecheck, build, 13 tests, seed×2. | c97352d |
| 2026-06-24 | plan | Phase 2/2.5 re-verify: no new Tier-1 blockers. Folded refinements — per-user rate-limit keying happens pre-auth (key off JWT sub in keyGenerator); soft-delete filters in list queries only (fetch-by-id/restore unaffected, no global Prisma hide); add `NotificationType`/`NotificationChannel` enums in 2A; `updateTicket` returns events to publish after commit; members endpoint enhances the Phase-1 route (+avatarUrl, initials fallback); E2E cross-user notification asserted via API. | fa3872e |
| 2026-06-23 | plan | Phase 2 audit (7-dim workflow, 62 findings) → folded Tier-1 fixes into plan: notification IDOR scoping, org-bounded @mention + server sanitize, invite token entropy/single-use/role-cap, sort whitelist + cursor tiebreaker, cross-scope validation, publish-after-commit + transactional create + `updateTicket` service, position scheme, onDelete + Label org relation, WS handshake hardening + self-echo dedupe + refetch-on-reconnect + shared `WSMessage`, public/gated routing, members endpoint, graceful shutdown, zod-provider scope, per-user rate limit. Defined sub-stages 2A–2E. Split **Phase 2.5 (UX hardening)**; dropped `bulk-update`; adopted shadcn. Updated phase-2, new phase-2.5, 03/04/06/07 refs, README, PROGRESS. | 37f5681 |
| 2026-06-23 | plan | Phase 2 blockers resolved in plan: (1) shared jose WS verifier + @fastify/websocket v11 `(socket,req)` signature; (2) lazy event bus init/dispose (tests use Redis); (3) atomic ticket numbering via `Project.ticketCounter`; (4) `Project.key` for AGP-42 + migration backfill; (5) `fastify-type-provider-zod` for validation+Swagger. Updated phase-2, 03-data-models, 04-api-reference. | 37f5681 |
| 2026-06-23 | plan | Phase 2 scope round 2: invite links, due date, soft-delete, search/filter/sort + cursor pagination, deep-link ticket route, optimistic UI/toasts/skeletons, quick-add + Cmd-K, markdown+@mention (DOMPurify), presence, in-app notification bell (WS user rooms → assignee/creator/watchers/mentioned), Swagger + /ready + seed, dark mode, i18n scaffold, mobile, Playwright E2E. Models: `OrgInvite` + in-app `Notification`, ticket `dueDate`/`archivedAt`. Decisions kept: org=project access, attachments deferred. | ae75857 |
| 2026-06-23 | plan | Phase 2 scope additions (feedback): clean/smooth/creative UI guideline, JIRA-style quick status change, assignee, watchers/CC, activity timeline, completion progress bar. Added `TicketWatcher` + `TicketActivity` models + watcher/activity endpoints. Not yet implemented. | 585ff49 |
| 2026-06-23 | plan | Re-sequenced phases: **Phase 2 = PM Core**, **Phase 3 = Deployment + CI/CD** (swapped). Renamed phase files + updated all headings, cross-refs, links, README flow/index, PROGRESS. | 9528d39 |
| 2026-06-23 | P1/E | Stage E (tests): hermetic auth harness (jose RSA keypair + in-test JWKS/OIDC stand-in, no Keycloak), Vitest globalSetup (creates+migrates `agentpm_test`) + per-worker truncation. Suites: auth middleware (6), organizations (4), projects (2) + health (1) = 13 green. Removed temp debug log. **Phase 1 complete.** | 8de7afe |
| 2026-06-23 | P1/D | Stage D (frontend): keycloak-js auth (login/signup, PKCE, token refresh), auth-gated React Router + Layout, typed API client (token attach + retry-on-401), Dashboard (orgs + create) + OrgProjects (projects + create) via React Query. Verified in-browser by user: signup → create org (OWNER) → create project. shadcn deferred (plain Tailwind). | 140d01c |
| 2026-06-23 | P1/C | Stage C (platform CRUD): Organizations CRUD + members (creator→OWNER, last-owner guard, add-by-email), Projects CRUD; shared authz (`assertOrgRole`/`requireOrgRole`/RBAC), slug helper, global error handler (ApiError + ZodError→400). Verified with real tokens: CRUD, validation 400, last-owner 400, non-member 403. | 2560397 |
| 2026-06-23 | P1/B | Stage B (API auth): @fastify/jwt + get-jwks JWKS verification (iss/aud); issuer vs JWKS host decoupled (no /etc/hosts). `requireAuth` + JIT User provisioning, `requireOrgRole` + RBAC, `GET`/`PATCH /api/me`. Verified with real Keycloak token: 401/tampered→401, valid→200, PATCH ok, idempotent (1 row). | 8f6d5c6 |
| 2026-06-23 | P1/A | Fix: local dev = plain HTTP (no TLS). Added dev-only `keycloak-init` (shares KC netns, sets master realm `sslRequired=NONE` on every up) so the admin console works over HTTP; not in prod overlay (prod keeps HTTPS via Caddy). Synced ref 12. | beac2c9 |
| 2026-06-23 | P1/A | Fix: moved prod Keycloak flags (start --optimized, KC_HOSTNAME, KC_PROXY_HEADERS, KC_HTTP_ENABLED) out of compose base → dev base now `start-dev`. Resolves admin-console "HTTPS required" on localhost. Synced ref 12 (base dev-safe; prod flags in prod overlay). | beac2c9 |
| 2026-06-23 | P1/A | Stage A scaffold: monorepo, Dockerfiles, compose (base+dev), Postgres init, Keycloak realm, Prisma schema+init migration, Fastify `/health`, Vite/React/Tailwind shell. Verified: install, typecheck, build, test, `docker compose up` (5 services green), `/health` 200, realm imported, migration applied. | beac2c9 |
| 2026-06-23 | — | Progress tracker created; repo not yet scaffolded | — |
